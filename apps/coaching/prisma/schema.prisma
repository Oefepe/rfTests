generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model account {
  id                             Int                          @id @default(autoincrement())
  name                           String?                      @db.VarChar(255)
  companyName                    String?                      @db.VarChar(255)
  accountHolder                  Int?
  address                        String?                      @db.VarChar(255)
  suite                          String?                      @db.VarChar(255)
  city                           String?                      @db.VarChar(255)
  stateId                        Int                          @db.UnsignedInt
  countryId                      Int                          @db.UnsignedInt
  zip                            String?                      @db.VarChar(20)
  url                            String?                      @db.VarChar(255)
  status                         Int
  isBillingInfoSet               Boolean?                     @default(false)
  accountLevel                   Int?
  accountSegment                 Int                          @default(1)
  ignoreAccountPayment           account_ignoreAccountPayment @default(VALUE_0)
  passThrough                    Boolean                      @default(false)
  freeTrial                      Boolean?                     @default(false)
  promoTrial                     Boolean?                     @default(false)
  paidTrialAmount                Decimal?                     @default(0.00) @db.Decimal(9, 2)
  paidTrialDays                  Int?                         @default(0) @db.SmallInt
  trial                          account_trial                @default(VALUE_0)
  trialPeriod                    Int                          @default(0)
  testAccount                    account_testAccount          @default(VALUE_0)
  trialUserEnabled               account_trialUserEnabled     @default(VALUE_0)
  userTrialPeriod                Int                          @default(0)
  dateCreated                    DateTime                     @default(now()) @db.Timestamp(0)
  createdBy                      Int
  dateModified                   DateTime                     @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  modifiedBy                     Int?
  wistiaProjectId                String?                      @db.VarChar(100)
  userToken                      String?                      @db.MediumText
  myWeeklyStats                  Boolean                      @default(true)
  enableMyWeeklyStats            Boolean?                     @default(true)
  levelOnOffFlag                 Boolean                      @default(false)
  grayedOutOnOff                 Boolean                      @default(false)
  traningModuleOnOffFlag_        Int                          @default(0) @map("‘traningModuleOnOffFlag’")
  enableTrainingModules          Int?                         @default(0)
  isCancelPremiumService         Boolean?                     @default(false)
  premiumCancelledOn             DateTime?                    @default(dbgenerated("'0000-00-00'")) @db.Date
  teammatesOnOffFlag             Boolean                      @default(false)
  enableEventModules             Int                          @default(0)
  enableLeadBuying               Boolean                      @default(false)
  enableResourceText             Boolean?                     @default(false)
  allowFreeUser                  Boolean?                     @default(true)
  allowStandardUserOnSignUp      Boolean?                     @default(true)
  enablePromoModules             Int                          @default(1)
  enableAchievement              Boolean?                     @default(false)
  enableTrackingCodeUsers        Boolean                      @default(false)
  enableTrackingCodeAdmins       Boolean                      @default(true)
  googleTrackingCode             String?                      @db.VarChar(100)
  gtmTrackingCode                String?                      @db.VarChar(100)
  fbTrackingCode                 String?                      @db.VarChar(100)
  enablePushNotification         Boolean                      @default(false)
  personalResourcesOnOffFlag     Boolean                      @default(false)
  enableConsentManage            Boolean?                     @default(true)
  enableContactTransfer          Boolean                      @default(false)
  personalEventsOnOffFlag        Boolean                      @default(false)
  enablePersonalResourceForStats Boolean                      @default(false)
  enableContactJourney           Boolean                      @default(false)
  enableYBR                      Boolean?                     @default(true)
  enableInitial48hEmail          Boolean                      @default(false)
  enableChargify                 Int                          @default(0) @db.TinyInt
  enableBillingSystem            Int?                         @default(0) @db.TinyInt
  enableDigitalBizCard           Boolean?                     @default(true)
  enableTasksModule              Boolean?                     @default(false)
  enableNewVideoPage             Boolean?                     @default(false)
  bizCardCoverImage              String?                      @db.VarChar(100)
  forceAppVersionUpdate          Int?                         @default(1) @db.TinyInt
  coachingModule                 Int                          @default(0) @db.UnsignedTinyInt
  enableSmartConnector           Boolean                      @default(false)

  @@index([accountLevel], map: "accountLevel")
  @@index([accountSegment], map: "accountSegment")
  @@index([createdBy], map: "createdBy")
  @@index([modifiedBy], map: "modifiedBy")
  @@index([stateId], map: "stateId")
  @@index([status], map: "status")
  @@fulltext([companyName], map: "companyName")
  @@fulltext([name], map: "name")
}

model accountAwardType {
  id   Int     @id @default(autoincrement())
  name String? @db.VarChar(100)

  @@fulltext([name], map: "name")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountBillingActivtyTypes {
  id          Int     @id @default(autoincrement())
  description String  @db.VarChar(100)
  searchValue Int     @db.TinyInt
  userType    String? @db.VarChar(255)
  userStatus  Int?    @db.UnsignedInt

  @@index([description], map: "description")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountBillingComponents {
  id               Int      @id @default(autoincrement())
  accountId        Int
  billingProductId Int
  componentId      Int
  pricePointId     Int
  created          DateTime @default(now()) @db.Timestamp(0)
  createdBy        Int?     @db.UnsignedInt
  modified         DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)
  modifiedBy       Int?     @db.UnsignedInt

  @@index([accountId], map: "abcomponents_accountId")
  @@index([billingProductId], map: "abcomponents_billingProductId")
  @@index([componentId], map: "abcomponents_componentId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountBillingCustomers {
  id                     Int                                    @id @default(autoincrement())
  firstName              String                                 @db.VarChar(30)
  lastName               String                                 @db.VarChar(30)
  email                  String                                 @db.VarChar(50)
  userId                 Int                                    @db.UnsignedInt
  accountId              Int                                    @db.UnsignedInt
  groupId                Int                                    @db.UnsignedInt
  portalCustomerId       String?                                @db.VarChar(50)
  portalPaymentProfileId String?                                @db.VarChar(50)
  payerId                Int                                    @db.UnsignedInt
  payingUserType         accountBillingCustomers_payingUserType @default(user)
  created                DateTime                               @default(now()) @db.Timestamp(0)
  updated                DateTime                               @default(now()) @db.Timestamp(0)

  @@unique([payerId, payingUserType], map: "billing_customer_payerId_payingUserType_unique")
  @@index([accountId], map: "billing_customer_accountId")
  @@index([groupId], map: "billing_customer_groupId")
  @@index([payerId], map: "billing_customer_payerId")
  @@index([userId], map: "billing_customer_userId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountBillingLogs {
  accountId                Int      @id @db.UnsignedInt
  planChangedTo            String?  @db.VarChar(100)
  planChangeRunStatus      Boolean  @default(false)
  activeUsersPlanChangeLog String?  @db.Text
  modifiedAt               DateTime @default(now()) @db.Timestamp(0)
}

model accountBillingOptionType {
  id   Int    @id @default(autoincrement())
  name String @db.VarChar(20)

  @@index([name], map: "name")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountBillingPayerTypes {
  id        Int     @id @default(autoincrement())
  name      String  @db.VarChar(20)
  shortName String? @db.VarChar(200)
  title     String? @db.VarChar(200)

  @@index([name], map: "name")
}

model accountBillingPayingForTypes {
  id          Int    @id @default(autoincrement())
  payerTypeId Int
  payingFor   String @db.VarChar(200)

  @@index([payerTypeId], map: "payerTypeId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountBillingPlans {
  id                           Int                             @id @default(autoincrement())
  accountId                    Int
  siteId                       Int
  payerTypeId                  Int
  payingUser                   accountBillingPlans_payingUser?
  allowStandardUserOnSignUp    Boolean?                        @default(true)
  allowStandardOptionsOnSignup Int?                            @default(1) @db.TinyInt
  showCouponInAccBillingPage   Int?                            @default(0) @db.TinyInt
  showCouponAtAccCreation      Int?                            @default(0) @db.TinyInt
  standardUserActions          Int                             @default(0) @db.TinyInt
  payingForId                  Int?                            @db.TinyInt
  productFamilyId              Int
  trialDays                    Int                             @db.TinyInt
  trialCost                    Float
  oneTimeSetupFee              Float
  disabledUserMessage          String?                         @db.Text
  upgradeUserUrl               String?                         @db.Text
  upgradeUserMessage           String?                         @db.Text
  created                      DateTime                        @default(now()) @db.Timestamp(0)
  createdBy                    Int?                            @db.UnsignedInt
  modified                     DateTime                        @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)
  modifiedBy                   Int?                            @db.UnsignedInt

  @@index([accountId], map: "abp_accountId")
  @@index([payerTypeId], map: "abp_payerTypeId")
  @@index([siteId], map: "abp_siteId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountBillingProducts {
  id            Int                                 @id @default(autoincrement())
  accountId     Int
  billingPlanId Int
  productId     Int
  pricePointId  Int
  intervalUnit  accountBillingProducts_intervalUnit @default(month)
  interval      Int                                 @db.TinyInt
  created       DateTime                            @default(now()) @db.Timestamp(0)
  createdBy     Int?                                @db.UnsignedInt
  modified      DateTime                            @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)
  modifiedBy    Int?                                @db.UnsignedInt

  @@index([accountId], map: "abproducts_accountId")
  @@index([billingPlanId], map: "abproducts_billingPlanId")
  @@index([productId], map: "abproducts_productId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountBillingSites {
  id                  Int      @id @default(autoincrement())
  name                String   @db.VarChar(50)
  url                 String   @db.VarChar(50)
  apiKey              String   @db.VarChar(200)
  chargifyJsPublicKey String   @db.VarChar(100)
  created             DateTime @default(now()) @db.Timestamp(0)
  createdBy           Int?     @db.UnsignedInt
  modified            DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)
  modifiedBy          Int?     @db.UnsignedInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountBillingSubscriptions {
  id                     Int                                      @id @default(autoincrement())
  isActive               Int                                      @default(0) @db.TinyInt
  customerId             Int                                      @unique(map: "customerId") @db.UnsignedInt
  payingSubscriptionId   Int                                      @default(0) @db.UnsignedInt
  portalSubscriptionId   String?                                  @default("") @db.VarChar(50)
  portalCustomerId       String?                                  @default("") @db.VarChar(50)
  balanceInCents         Decimal                                  @default(0.00) @db.Decimal(8, 2)
  totalRevenueInCents    Decimal                                  @default(0.00) @db.Decimal(8, 2)
  currentPeriodStartedAt DateTime?                                @db.Timestamp(0)
  currentPeriodEndsAt    DateTime?                                @db.Timestamp(0)
  isTrial                Int                                      @default(0) @db.TinyInt
  trialStartedAt         DateTime?                                @db.Timestamp(0)
  trialEndedAt           DateTime?                                @db.Timestamp(0)
  cancelAtEndOfPeriod    Int?                                     @default(0) @db.TinyInt
  canceledAt             DateTime?                                @db.Timestamp(0)
  cancelFeedback         String?                                  @db.Text
  cancelReasonCode       String?                                  @db.Text
  portalProductId        String?                                  @default("") @db.VarChar(50)
  intervalUnit           accountBillingSubscriptions_intervalUnit @default(month)
  interval               Int                                      @db.TinyInt
  paymentType            String?                                  @db.VarChar(20)
  payingUser             String?                                  @db.VarChar(50)
  failureCount           Int?                                     @default(0) @db.TinyInt
  created                DateTime                                 @default(now()) @db.Timestamp(0)
  updated                DateTime                                 @default(now()) @db.Timestamp(0)

  @@index([payingSubscriptionId], map: "billingPayingSubscriptionId")
  @@index([portalSubscriptionId], map: "billingPortalSubscriptionId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountBillingTransactions {
  id                   Int                             @id @default(autoincrement())
  eventId              String                          @db.VarChar(100)
  type                 accountBillingTransactions_type
  paymentId            String                          @db.VarChar(100)
  customerId           Int                             @db.UnsignedInt
  subscriptionId       String?                         @default("") @db.VarChar(50)
  amount               Float?                          @default(0.00)
  portalCustomerId     String?                         @default("") @db.VarChar(50)
  portalSubscriptionId String?                         @default("") @db.VarChar(50)
  paymentTime          DateTime                        @default(now()) @db.Timestamp(0)
  isCanceled           Boolean?                        @default(false)
  created              DateTime                        @default(now()) @db.Timestamp(0)
  updated              DateTime                        @default(now()) @db.Timestamp(0)

  @@unique([eventId, type], map: "billingTransactions_eventId_type")
  @@index([customerId], map: "customerId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountBranding {
  id                                        Int       @id @default(autoincrement())
  accountId                                 Int       @unique(map: "accountId")
  dashboardLogo                             String?   @db.VarChar(100)
  mobileLogo                                String?   @db.VarChar(100)
  videoPageBackgroundColor                  String?   @db.VarChar(100)
  favIcon                                   String?   @db.VarChar(100)
  videoBackgroundType                       Boolean   @default(false)
  videoPageLogo                             String?   @db.VarChar(100)
  eventLogo                                 String?   @db.VarChar(100)
  videoLogo                                 String?   @db.VarChar(100)
  isShowColorToMobile                       Boolean   @default(false)
  primaryColor                              String?   @default("#77B800") @db.VarChar(20)
  primaryColorOffset                        String?   @default("#FFFFFF") @db.VarChar(20)
  secondaryColor                            String?   @default("#220090") @db.VarChar(20)
  secondaryColorOffset                      String?   @default("#FFFFFF") @db.VarChar(20)
  tertiaryColor                             String?   @default("#0096db") @db.VarChar(20)
  tertiaryColorOffset                       String?   @default("#FFFFFF") @db.VarChar(20)
  domainName                                String?   @db.VarChar(200)
  contestTermsConditionsUrl                 String?   @db.VarChar(200)
  supportLink                               String?   @db.VarChar(200)
  useRFSupportLink                          Boolean   @default(true)
  reBrand                                   String?   @db.VarChar(200)
  reBrandAdditionalNotificationEmail        String?   @db.VarChar(100)
  reBrandRepIdToolTip                       String?   @db.MediumText
  reBrandAdditionalNotificationEmailToolTip String?   @db.MediumText
  reBrandRepId2                             String?   @db.VarChar(200)
  reBrandRepId2ToolTip                      String?   @db.MediumText
  reBrandRepId3                             String?   @db.VarChar(200)
  reBrandRepId3ToolTip                      String?   @db.Text
  reBrandRepId4                             String?   @db.VarChar(200)
  reBrandRepId4ToolTip                      String?   @db.Text
  reBrandRepId5                             String?   @db.VarChar(200)
  reBrandRepId5ToolTip                      String?   @db.Text
  reBrandRepId6                             String?   @db.VarChar(200)
  reBrandRepId6ToolTip                      String?   @db.Text
  reBrandRepId7                             String?   @db.VarChar(200)
  reBrandRepId7ToolTip                      String?   @db.Text
  reBrandRepId8                             String?   @db.VarChar(200)
  reBrandRepId8ToolTip                      String?   @db.Text
  upgradeInformationForFreeUsers            String?   @db.MediumText
  showFreeUserUpgradeInfo                   Boolean   @default(true)
  created                                   DateTime  @default(now()) @db.Timestamp(0)
  createdBy                                 Int?      @default(0) @db.UnsignedInt
  modified                                  DateTime? @default(now()) @db.Timestamp(0)
  modifiedBy                                Int?      @default(0) @db.UnsignedInt
  userType                                  Boolean?  @default(false)
  recruitRebrandText                        String?   @db.VarChar(100)
  customerRebrandText                       String?   @db.VarChar(100)
  removeRFLogoOnDashboard                   Boolean   @default(false)
  useDomainToSendEmail                      Boolean?  @default(false)
  domainToSendEmail                         String?   @default("") @db.VarChar(50)
  hidePoweredByRFLogoOnResources            Boolean   @default(false)
  appName                                   String?   @default("RapidFunnel") @db.VarChar(200)
  appStoreUrl                               String?   @default("https://apps.apple.com/us/app/rapidfunnel/id948835721") @db.VarChar(200)
  googlePlayUrl                             String?   @default("https://play.google.com/store/apps/details?id=com.rapidfunnel") @db.VarChar(200)
  appIconImage                              String?   @default("rapidfunnel-2.jpg") @db.VarChar(200)
  googleTranslateBar                        Boolean?  @default(false)
  userFlowResId                             BigInt?   @db.UnsignedBigInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountBroadcastEmails {
  id                 Int                                  @id @default(autoincrement())
  sentTime           DateTime                             @db.DateTime(0)
  sentBy             Int
  fromEmail          String?                              @db.VarChar(255)
  subject            String                               @db.VarChar(255)
  htmlContent        String?                              @db.MediumText
  textContent        String?                              @db.MediumText
  isDraft            Int?                                 @default(0) @db.TinyInt
  recipientType      accountBroadcastEmails_recipientType
  recipients         String?                              @db.LongText
  recipientCount     Int?
  incentiveId        String?                              @db.LongText
  searchString       String?                              @db.VarChar(300)
  updateTempFlag     String?                              @default("yes") @db.VarChar(10)
  userBroadcast      Int                                  @default(0)
  broadcastByRfAdmin Boolean?                             @default(false)
  broadcastMethod    Boolean?                             @default(true)
  domainName         String?                              @db.VarChar(150)
  accepted           Int?                                 @default(0) @db.UnsignedInt
  delivered          Int?                                 @default(0) @db.UnsignedInt
  bounced            Int?                                 @default(0) @db.UnsignedInt
  uniqueOpened       Int?                                 @default(0) @db.UnsignedInt
  uniqueClicked      Int?                                 @default(0) @db.UnsignedInt
  status             Boolean?                             @default(false)
  timezoneId         Int                                  @default(425) @db.UnsignedInt
  broadcastSchedule  Int?                                 @default(0) @db.TinyInt

  @@fulltext([subject], map: "subject")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountBroadcastTempMail {
  id               Int                                    @id @default(autoincrement())
  accountId        Int
  broadcastMailId  Int
  isDraft          Boolean?                               @default(false)
  recipientType    accountBroadcastTempMail_recipientType
  recipients       String?                                @db.MediumText
  total            Int?
  failedRecipients String?                                @db.MediumText
  totalFailed      Int?                                   @default(0) @db.UnsignedTinyInt
  sendStatus       Boolean                                @default(false)
  inProgress       Boolean                                @default(false)
  isDelete         Boolean                                @default(false)
  created          DateTime                               @db.DateTime(0)
  modified         DateTime                               @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  isSentToSqs      Boolean                                @default(true)

  @@index([broadcastMailId], map: "broadcastMailId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountBusinessCard {
  id                  Int       @id @default(autoincrement()) @db.UnsignedInt
  accountId           Int
  coverImage          String?   @db.VarChar(100)
  showName            Boolean   @default(true)
  showJobTitle        Boolean   @default(true)
  showPhone           Boolean   @default(true)
  showMobile          Boolean   @default(true)
  showEmail           Boolean   @default(true)
  showWebsite         Boolean   @default(true)
  showLocation        Boolean   @default(true)
  showFacebook        Boolean   @default(true)
  showLinkedIn        Boolean   @default(true)
  showTwitter         Boolean   @default(true)
  showInstagram       Boolean   @default(true)
  showWhatsApp        Boolean   @default(false)
  showTikTok          Boolean   @default(false)
  showPublicPageLogo  Boolean   @default(false)
  showGroupLogo       Boolean   @default(false)
  btnLabel1           String?   @default("") @db.VarChar(255)
  linkedResourceId1   Int       @default(0)
  openInNewTab1       Boolean   @default(true)
  btnLabel2           String?   @default("") @db.VarChar(255)
  linkedResourceId2   Int       @default(0)
  openInNewTab2       Boolean   @default(true)
  btnLabel3           String?   @default("") @db.VarChar(255)
  linkedResourceId3   Int       @default(0)
  openInNewTab3       Boolean   @default(true)
  btnLabel4           String?   @default("") @db.VarChar(255)
  linkedResourceId4   Int       @default(0)
  openInNewTab4       Boolean   @default(true)
  btnLabel5           String?   @default("") @db.VarChar(255)
  linkedResourceId5   Int       @default(0)
  openInNewTab5       Boolean   @default(true)
  btnLabel6           String?   @default("") @db.VarChar(255)
  linkedResourceId6   Int       @default(0)
  openInNewTab6       Boolean   @default(true)
  btnLabel7           String?   @default("") @db.VarChar(255)
  linkedResourceId7   Int       @default(0)
  openInNewTab7       Boolean   @default(true)
  btnLabel8           String?   @default("") @db.VarChar(255)
  linkedResourceId8   Int       @default(0)
  openInNewTab8       Boolean   @default(true)
  btnLabel9           String?   @default("") @db.VarChar(255)
  linkedResourceId9   Int       @default(0)
  openInNewTab9       Boolean   @default(true)
  btnLabel10          String?   @default("") @db.VarChar(255)
  linkedResourceId10  Int       @default(0)
  openInNewTab10      Boolean   @default(true)
  created_at          DateTime? @db.Timestamp(0)
  updated_at          DateTime? @db.Timestamp(0)
  businessCardSubject String?   @db.VarChar(255)
  businessCardMessage String?   @db.Text

  @@index([accountId], map: "accountbusinesscard_accountid_index")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountCampaign {
  id                    Int                       @id @default(autoincrement())
  accountId             Int
  name                  String                    @db.VarChar(255)
  description           String?                   @db.LongText
  showInApp             accountCampaign_showInApp @default(VALUE_0)
  overrideExistingUsers Boolean                   @default(false)
  sendFromEmail         String?                   @db.VarChar(255)
  status                accountCampaign_status    @default(VALUE_1)
  languageCode          String?                   @default("en") @db.VarChar(25)
  reminderReview        Boolean?                  @default(false)
  reminderEmail         String?                   @db.Text
  reminderSendDate      DateTime?                 @default(dbgenerated("'0000-00-00'")) @db.Date
  freeCampaign          Boolean                   @default(false)
  allLevelSelected      Boolean                   @default(false)
  campaignCategoryId    Int?                      @default(0)
  created               DateTime                  @default(now()) @db.Timestamp(0)
  createdBy             Int?
  modified              DateTime                  @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  modifiedBy            Int?
  cmpAppUpdateProgress  Int?                      @default(0) @db.TinyInt

  @@index([accountId], map: "accountId")
  @@index([createdBy], map: "createdBy")
  @@index([modifiedBy], map: "modifiedBy")
  @@fulltext([name], map: "name")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountCampaignCategories {
  id          Int      @id @default(autoincrement()) @db.UnsignedInt
  accountId   Int
  parentId    Int?     @default(0)
  name        String   @db.VarChar(255)
  description String   @db.Text
  priority    Int?     @default(0)
  created     DateTime @default(now()) @db.Timestamp(0)
  createdBy   Int?     @db.UnsignedInt
  modified    DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)
  modifiedBy  Int?     @db.UnsignedInt

  @@index([accountId], map: "accountId")
  @@index([name(length: 191)], map: "name")
}

model accountCampaignContact {
  accountCampaignId Int
  accountContactId  Int
  channel           accountCampaignContact_channel @default(web)
  createdBy         String                         @db.VarChar(100)
  created           DateTime                       @default(now()) @db.DateTime(0)

  @@id([accountCampaignId, accountContactId])
  @@index([accountContactId], map: "accountContactId")
}

model accountCampaignContactCount {
  pkId              Int     @id @default(autoincrement()) @db.UnsignedInt
  accountId         Int     @db.UnsignedInt
  accountUserId     Int     @db.UnsignedInt
  accountCampaignId Int     @db.UnsignedInt
  contactCount      BigInt?

  @@unique([accountId, accountUserId, accountCampaignId], map: "accountUserCampaignId")
  @@index([accountCampaignId], map: "accountCampaignId")
  @@index([accountId], map: "accountId")
  @@index([accountUserId], map: "accountUserId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountCampaignContactEmail {
  id         Int                                    @id @default(autoincrement())
  accountId  Int
  campaignId Int
  contactId  Int
  emailId    Int
  sendOnDate DateTime                               @db.Date
  sendStatus accountCampaignContactEmail_sendStatus @default(VALUE_0)
  created    DateTime                               @default(now()) @db.Timestamp(0)

  @@index([accountId], map: "accountId")
  @@index([contactId], map: "contactId")
  @@index([emailId], map: "emailId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountCampaignEmail {
  id          Int                            @id @default(autoincrement())
  campaignId  Int
  title       String?                        @db.VarChar(255)
  content     String?                        @db.MediumText
  textContent String?                        @db.MediumText
  type        Boolean                        @default(false)
  days        Int                            @db.SmallInt
  sendingDate DateTime?                      @db.Date
  status      accountCampaignEmail_status    @default(draft)
  isDeleted   accountCampaignEmail_isDeleted @default(VALUE_0)
  created     DateTime                       @default(now()) @db.Timestamp(0)
  createdBy   Int?
  modified    DateTime?                      @db.Timestamp(0)
  modifiedBy  Int?

  @@index([campaignId], map: "campaignId")
  @@index([createdBy], map: "createdBy")
  @@index([modifiedBy], map: "modifiedBy")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountCampaignEmailLogs {
  accountId       Int      @db.UnsignedInt
  campaignId      Int      @db.UnsignedInt
  campaignEmailId Int      @db.UnsignedInt
  openRate        Decimal? @default(0.00) @db.Decimal(6, 2)
  clickRate       Decimal? @default(0.00) @db.Decimal(6, 2)
  optOutRate      Decimal? @default(0.00) @db.Decimal(6, 2)
  complaintRate   Decimal? @default(0.00) @db.Decimal(6, 2)
  avgVidWatched   Decimal? @default(0.00) @db.Decimal(6, 2)
  created         DateTime @default(now()) @db.Timestamp(0)
  updated         DateTime @default(now()) @db.Timestamp(0)

  @@id([accountId, campaignId, campaignEmailId])
}

model accountCampaignGroup {
  pkId              Int @id @default(autoincrement()) @db.UnsignedInt
  accountCampaignId Int
  accountGroupId    Int

  @@unique([accountCampaignId, accountGroupId], map: "campaignGroup")
  @@index([accountGroupId], map: "accountGroupId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountCampaignLogs {
  accountId     Int      @db.UnsignedInt
  campaignId    Int      @db.UnsignedInt
  sentMailCount Int      @db.UnsignedInt
  optInRate     Decimal? @default(0.00) @db.Decimal(6, 2)
  optOutRate    Decimal? @default(0.00) @db.Decimal(6, 2)
  openRate      Decimal? @default(0.00) @db.Decimal(6, 2)
  clickRate     Decimal? @default(0.00) @db.Decimal(6, 2)
  totalContacts Int?     @default(0) @db.UnsignedInt
  created       DateTime @default(now()) @db.Timestamp(0)
  updated       DateTime @default(now()) @db.Timestamp(0)

  @@id([accountId, campaignId])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountCampaignNotificationEmail {
  id                          Int      @id @default(autoincrement())
  accountCampaignId           Int
  accountUserId               Int
  additionalNotificationEmail String   @db.VarChar(200)
  created                     DateTime @default(now()) @db.Timestamp(0)
  updated                     DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountCampaignTempEmail {
  id              Int       @id @default(autoincrement())
  accountId       Int
  campaignId      Int
  campaignEmailId Int
  days            Int
  sendingDate     DateTime? @db.Date
  type            Int       @default(0) @db.TinyInt

  @@index([campaignEmailId], map: "campaignEmailId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountCardProfile {
  id                           Int       @id @default(autoincrement())
  accountId                    Int       @unique(map: "accountId")
  firstName                    String    @db.VarChar(255)
  middleName                   String?   @db.VarChar(255)
  lastName                     String?   @db.VarChar(255)
  email                        String    @db.VarChar(255)
  address                      String    @db.VarChar(255)
  suite                        String    @db.VarChar(255)
  city                         String    @db.VarChar(255)
  stateId                      Int?
  countryId                    Int?      @default(0)
  zip                          String    @db.VarChar(20)
  maskedCcNo                   String    @db.VarChar(30)
  ccType                       String    @db.VarChar(100)
  ccExpMonth                   Int       @db.SmallInt
  ccExpYear                    Int       @db.SmallInt
  maskedCvv                    String    @db.VarChar(5)
  authorizeProfileToken        String    @db.VarChar(100)
  authorizePaymentProfileToken String?   @db.VarChar(100)
  created                      DateTime  @default(now()) @db.Timestamp(0)
  createdBy                    Int?
  modified                     DateTime  @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  modifiedBy                   Int?
  firstCycleStartDate          DateTime? @db.Date
  lastPaymentDoneOn            DateTime? @db.Date
  nextPaymentOn                DateTime? @db.Date
  failureCount                 Int?      @default(0) @db.SmallInt
  lastAmountPaid               Float?    @db.Float
  lastTransactionId            Int?

  @@index([createdBy], map: "createdBy")
  @@index([modifiedBy], map: "modifiedBy")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountCollateral {
  id          Int                         @id @default(autoincrement())
  accountId   Int
  name        String                      @db.VarChar(255)
  description String?                     @db.MediumText
  path        String?                     @db.MediumText
  created     DateTime                    @default(now()) @db.Timestamp(0)
  createdBy   Int
  modified    DateTime?                   @db.Timestamp(0)
  modifiedBy  Int?
  isDeleted   accountCollateral_isDeleted @default(VALUE_0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountCommissionPercentage {
  pkId             Int    @id @default(autoincrement()) @db.UnsignedInt
  commissionUserId Int    @db.UnsignedInt
  commissionOrder  Int    @default(0) @db.UnsignedTinyInt
  minUsersCount    Int    @default(0) @db.UnsignedInt
  percentage       Float? @default(0) @db.Float

  @@index([commissionUserId], map: "commissionUserId")
}

model accountCommissionType {
  id   Int    @id @default(autoincrement())
  name String @db.VarChar(255)
}

model accountCommissionUser {
  id               Int      @id @default(autoincrement())
  accountId        Int
  firstName        String   @db.VarChar(100)
  lastName         String?  @db.VarChar(100)
  email            String   @db.VarChar(250)
  phoneNumber      String?  @db.VarChar(100)
  commissionTypeId Int
  percentage       Float    @db.Float
  created          DateTime @default(now()) @db.Timestamp(0)
  createdBy        Int
  modified         DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  modifiedBy       Int?

  @@index([accountId], map: "accountId")
}

model accountCommissionUserNotes {
  id                      Int      @id @default(autoincrement())
  accountCommissionUserId Int
  note                    String   @db.Text
  created                 DateTime @default(now()) @db.Timestamp(0)
  createdBy               Int
  modified                DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  modifiedBy              Int?

  @@index([accountCommissionUserId], map: "accountCommissionUserId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountContact {
  id                          Int                     @id @default(autoincrement())
  accountId                   Int
  firstName                   String                  @db.VarChar(100)
  lastName                    String                  @db.VarChar(100)
  email                       String?                 @db.VarChar(250)
  homeEmail                   String?                 @db.VarChar(250)
  workEmail                   String?                 @db.VarChar(250)
  otherEmail                  String?                 @db.VarChar(250)
  phone                       String?                 @db.MediumText
  home                        String?                 @db.MediumText
  work                        String?                 @db.MediumText
  other                       String?                 @db.MediumText
  mobile                      String?                 @db.VarChar(20)
  address1                    String?                 @db.VarChar(200)
  address2                    String?                 @db.VarChar(200)
  city                        String?                 @db.VarChar(50)
  stateId                     Int?                    @db.UnsignedInt
  countryId                   Int?                    @db.UnsignedInt
  zip                         String?                 @db.VarChar(20)
  company                     String?                 @db.VarChar(100)
  title                       String?                 @db.VarChar(200)
  interest                    Boolean?
  note                        String?                 @db.Text
  noteCount                   Int                     @default(0) @db.MediumInt
  channel                     accountContact_channel? @default(Web_Application)
  optInSend                   Boolean                 @default(false)
  optIn                       Boolean                 @default(false)
  optInDate                   DateTime?               @db.Timestamp(0)
  optInResend                 Boolean                 @default(false)
  optOut                      Boolean?                @default(false)
  optOutDate                  DateTime?               @db.Timestamp(0)
  engagedDate                 DateTime?               @db.Timestamp(0)
  exposureDate                DateTime?               @db.Timestamp(0)
  status                      Float?                  @db.Float
  campaignStatus              Boolean?                @default(false)
  resourceStatus              Boolean?                @default(false)
  yearWeekOfCreated           Int?
  actualCreated               DateTime?               @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  created                     DateTime                @default(now()) @db.Timestamp(0)
  createdBy                   Int                     @db.UnsignedInt
  createdByName               String?                 @db.VarChar(100)
  modified                    DateTime                @default(now()) @db.Timestamp(0)
  modifiedBy                  Int
  isLastUpdatedByRF           Boolean?                @default(false)
  updatedByRFAdminId          Int?
  customContact               Boolean?                @default(false)
  optInSendDate               DateTime?               @db.Timestamp(0)
  ratings                     Boolean?                @default(false)
  points                      Int?                    @default(0) @db.TinyInt
  campaignAssignTime          DateTime?               @db.Timestamp(0)
  campaignId                  Int?
  campaignName                String?                 @db.VarChar(100)
  contactTag                  String?                 @default("") @db.VarChar(255)
  profileImage                String?                 @db.VarChar(100)
  resourceFirstSentDate       DateTime?               @db.Timestamp(0)
  resourceFirstViewedDate     DateTime?               @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  contactFirstSentOptIn       DateTime?               @db.Timestamp(0)
  eventFirstSentDate          DateTime?               @db.Timestamp(0)
  eventFirstAcceptedDate      DateTime?               @db.Timestamp(0)
  internalId                  BigInt?                 @default(0) @db.UnsignedBigInt
  lastActivityOn              DateTime?               @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  isReward                    Boolean?                @default(false)
  hash                        String?                 @db.Char(128)
  countryPhoneCode            String?                 @db.VarChar(100)
  isTransferredContact        Boolean                 @default(false)
  reCaptchaScore              Float?                  @default(0) @db.Float
  industry                    String?                 @db.VarChar(100)
  assignedTo                  String?                 @db.VarChar(100)
  businessCardFirstSentDate   DateTime?               @db.DateTime(0)
  businessCardFirstViewedDate DateTime?               @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)
  userContactRelation         String?                 @db.VarChar(100)

  @@index([accountId, createdBy], map: "ac_accountId_createdBy")
  @@index([accountId], map: "accountContact_accountId")
  @@index([createdBy], map: "accountContact_createdBy")
  @@index([optInResend, optInSend, optInSendDate, status, accountId], map: "accountContact_engagedContact_index")
  @@index([yearWeekOfCreated], map: "accountContact_yearWeekOfCreated")
  @@index([campaignStatus], map: "campaignStatus_ac_index")
  @@index([channel], map: "channel")
  @@index([contactFirstSentOptIn], map: "contactFirstSentOptIn_ac_index")
  @@index([campaignName], map: "contact_campaignName")
  @@index([createdByName], map: "contact_createdByName")
  @@index([internalId], map: "contact_internalId")
  @@index([accountId, optIn, status], map: "countByOptIn")
  @@index([accountId, optInSend, optInResend, status], map: "countByOptInSend")
  @@index([email(length: 191)], map: "email")
  @@index([exposureDate], map: "exposureDate")
  @@index([firstName], map: "firstName")
  @@index([firstName, lastName], map: "firstname_lastname")
  @@index([resourceFirstSentDate], map: "resourceFirstSentDate_ac_index")
  @@index([resourceStatus], map: "resourceStatus_ac_index")
  @@fulltext([firstName, lastName], map: "firstName_2")
}

model accountContactActivities {
  id                 Int       @id @default(autoincrement())
  accountId          Int
  userId             Int?
  accountContactId   Int?
  contactEngagedDate DateTime? @db.DateTime(0)
  contactOptInDate   DateTime? @db.DateTime(0)
  eventAcceptDate    DateTime? @db.DateTime(0)
  created            DateTime? @db.DateTime(0)
  modified           DateTime? @db.DateTime(0)

  @@index([accountContactId], map: "accountContactId")
  @@index([accountId], map: "accountId")
  @@index([userId], map: "userId")
}

model accountContactAnalytics {
  id                   Int      @id @default(autoincrement())
  eventId              String   @unique(map: "watch_event_id") @db.VarChar(100)
  accountContactId     Int
  accountResourceId    Int
  resourceName         String   @default("") @db.VarChar(255)
  accountUserId        Int
  percentWatched       Float    @db.Float
  visitorKey           String   @db.VarChar(100)
  mediaHash            String   @db.VarChar(100)
  duration             Float    @db.Float
  created              DateTime @default(now()) @db.Timestamp(0)
  isPointLogged        Boolean? @default(false)
  contactHash          String?  @db.Char(128)
  isFcmSent            Boolean? @default(false)
  finalWatchUpdateDone Boolean? @default(false)

  @@index([contactHash], map: "con_hash")
  @@index([isPointLogged, finalWatchUpdateDone], map: "contactAnalytics_pointLog_finalWatchDone")
  @@fulltext([resourceName], map: "resourceName")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountContactConsents {
  id          Int       @id @default(autoincrement()) @db.UnsignedInt
  contactId   Int?
  contactHash String?   @db.Char(128)
  status      Boolean   @default(true)
  consent     String    @db.Text
  created     DateTime? @default(now()) @db.Timestamp(0)

  @@index([contactHash], map: "contactHash")
  @@index([contactId], map: "contactId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountContactConversion {
  id               Int      @id @default(autoincrement())
  accountContactId Int      @unique(map: "accountContactId")
  recruitFlag      Int      @default(0) @db.TinyInt
  customerFlag     Int      @default(0) @db.TinyInt
  createdTime      DateTime @default(now()) @db.Timestamp(0)
  updatedTime      DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountContactDeletes {
  id        Int      @id @default(autoincrement())
  contactId Int      @db.UnsignedInt
  accountId Int      @db.UnsignedInt
  userId    Int      @db.UnsignedInt
  created   DateTime @default(now()) @db.Timestamp(0)
  remarks   String?  @db.VarChar(100)

  @@index([userId], map: "userId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountContactEvent {
  id            Int      @id @default(autoincrement())
  accountId     Int
  userId        Int
  contactId     Int
  eventTitle    String   @db.VarChar(200)
  eventDate     DateTime @db.Date
  eventTime     DateTime @db.Time(0)
  eventReminder String   @default("0") @db.VarChar(200)
  eventNote     String?  @db.MediumText
  isDeleted     Boolean  @default(false)
  created       DateTime @default(now()) @db.Timestamp(0)
  modified      DateTime @default(now()) @db.Timestamp(0)
  internalId    BigInt?  @default(0) @db.UnsignedBigInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountContactEventEmail {
  id         Int      @id @default(autoincrement())
  contactId  Int
  eventId    Int
  sendTime   DateTime @db.DateTime(0)
  sendStatus Int      @default(0) @db.TinyInt
  created    DateTime @default(now()) @db.Timestamp(0)
  modified   DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)

  @@index([sendTime], map: "sendTime")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountContactJourney {
  id                  Int      @id @default(autoincrement())
  accountId           Int
  name                String   @db.VarChar(200)
  type                Boolean  @default(false)
  order               Int
  description         String?  @db.VarChar(255)
  color               Int      @default(1) @db.TinyInt
  created             DateTime @default(now()) @db.Timestamp(0)
  createdBy           Int?
  modified            DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  modifiedBy          Int?
  journeyDisplayOrder Int?     @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountContactMessageLog {
  id         Int                               @id @default(autoincrement()) @db.UnsignedInt
  accountId  Int                               @default(0)
  userId     Int                               @db.UnsignedInt
  contactId  Int                               @db.UnsignedInt
  subject    String?                           @default("") @db.VarChar(255)
  content    String?                           @db.Text
  status     accountContactMessageLog_status
  sentFrom   accountContactMessageLog_sentFrom @default(mobile)
  sentMethod Boolean                           @default(false)
  created    DateTime                          @default(now()) @db.Timestamp(0)

  @@index([accountId], map: "accountId")
  @@index([contactId], map: "contactId")
  @@index([userId], map: "userId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountContactNotes {
  id               Int      @id @default(autoincrement())
  accountContactId Int
  contactNote      String?  @db.Text
  created          DateTime @default(now()) @db.Timestamp(0)
  modified         DateTime @default(now()) @db.Timestamp(0)
  guestNote        Boolean? @default(false)
  internalId       BigInt?  @default(0) @db.UnsignedBigInt
  modified_bkp     DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)

  @@index([accountContactId], map: "contactIdNote")
  @@fulltext([contactNote], map: "contactNote")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountContactNotesDeletes {
  id        Int      @id @default(autoincrement())
  accountId Int      @db.UnsignedInt
  noteId    Int      @db.UnsignedInt
  contactId Int      @db.UnsignedInt
  userId    Int      @db.UnsignedInt
  created   DateTime @default(now()) @db.Timestamp(0)
  remarks   String?  @db.VarChar(100)

  @@index([userId], map: "userId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountContactPoint {
  id               Int  @id @default(autoincrement()) @db.UnsignedInt
  accountContactId Int  @unique(map: "accountContactId") @db.UnsignedInt
  points           Int? @db.UnsignedInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountContactPointLog {
  id               Int       @id @default(autoincrement()) @db.UnsignedInt
  accountContactId Int       @db.UnsignedInt
  points           Int?      @db.UnsignedInt
  commentText      String?   @db.VarChar(100)
  isPointDeduct    Boolean?  @default(false)
  created          DateTime  @default(now()) @db.Timestamp(0)
  modified         DateTime? @default(now()) @db.Timestamp(0)

  @@index([accountContactId], map: "contactPointLog_contactId")
  @@index([points], map: "contactPointLog_points")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountContactReminderDeletes {
  id         Int      @id @default(autoincrement())
  accountId  Int      @db.UnsignedInt
  reminderId Int      @db.UnsignedInt
  contactId  Int      @db.UnsignedInt
  userId     Int      @db.UnsignedInt
  created    DateTime @default(now()) @db.Timestamp(0)
  remarks    String?  @db.VarChar(100)

  @@index([userId], map: "userId")
}

model accountContactTag {
  id               Int      @id @default(autoincrement()) @db.UnsignedInt
  accountContactId Int?
  tagId            Int?
  modified         DateTime @default(now()) @db.Timestamp(0)

  @@index([accountContactId], map: "accountcontacttag_accountcontactid_index")
  @@index([tagId], map: "accountcontacttag_tagid_index")
}

model accountContactTransferLog {
  id               Int      @id @default(autoincrement()) @db.UnsignedInt
  accountContactId Int      @db.UnsignedInt
  senderUserId     Int      @db.UnsignedInt
  receiverUserId   Int      @db.UnsignedInt
  created          DateTime @default(now()) @db.Timestamp(0)

  @@index([accountContactId], map: "accountContactTransferLog_accountContactId_index")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountContact_new {
  id            Int                         @default(0)
  accountId     Int
  firstName     String                      @db.VarChar(100)
  lastName      String                      @db.VarChar(100)
  email         String?                     @db.VarChar(250)
  homeEmail     String?                     @db.VarChar(250)
  workEmail     String?                     @db.VarChar(250)
  otherEmail    String?                     @db.VarChar(250)
  phone         String?                     @db.Text
  home          String?                     @db.Text
  work          String?                     @db.Text
  other         String?                     @db.Text
  mobile        String?                     @db.VarChar(20)
  address1      String?                     @db.VarChar(200)
  address2      String?                     @db.VarChar(200)
  city          String?                     @db.VarChar(50)
  state         String?                     @db.VarChar(50)
  country       String?                     @db.VarChar(50)
  zip           String?                     @db.VarChar(20)
  company       String?                     @db.VarChar(100)
  title         String?                     @db.VarChar(200)
  interest      Boolean?
  note          String?                     @db.Text
  channel       accountContact_new_channel? @default(Web_Application)
  optInSend     Boolean                     @default(false)
  optIn         Boolean                     @default(false)
  optInDate     DateTime?                   @db.Timestamp(0)
  optInResend   Boolean                     @default(false)
  optOut        Boolean?                    @default(false)
  optOutDate    DateTime?                   @db.Timestamp(0)
  status        Boolean?
  created       DateTime                    @default(now()) @db.Timestamp(0)
  createdBy     Int
  modified      DateTime                    @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  modifiedBy    Int
  customContact Boolean?                    @default(false)
  optInSendDate DateTime?                   @db.Timestamp(0)

  @@ignore
}

model accountContestViewed {
  id          Int  @id @default(autoincrement())
  incentiveId Int? @db.UnsignedInt
  userId      Int? @db.UnsignedInt
  status      Int? @db.UnsignedTinyInt
}

model accountCourseGroup {
  accountCourseGroupId Int @id @default(autoincrement()) @db.UnsignedInt
  accountCourseId      Int @db.UnsignedInt
  accountGroupId       Int @db.UnsignedInt
}

model accountCourseLevel {
  accountCourseLevelId Int @id @default(autoincrement()) @db.UnsignedInt
  accountCourseId      Int @db.UnsignedInt
  accountLevelId       Int @db.UnsignedInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountCourses {
  id                 Int                    @id @default(autoincrement()) @db.UnsignedInt
  accountId          Int?                   @db.UnsignedInt
  name               String?                @db.VarChar(255)
  description        String?                @db.MediumText
  thumbnail          String?                @db.VarChar(255)
  availabilityType   Int?                   @db.UnsignedInt
  status             accountCourses_status? @default(VALUE_1)
  freeCourses        Boolean?               @default(false)
  courseDisplayOrder Int?                   @default(0) @db.UnsignedInt
  allLevelSelected   Boolean?               @default(false)
  allGroupSelected   Boolean                @default(false)
  currentModule      Int?                   @db.UnsignedInt
  moduleStartDate    DateTime?              @db.DateTime(0)
  moduleEndDate      DateTime?              @db.DateTime(0)
  created            DateTime?              @default(now()) @db.Timestamp(0)
  createdBy          Int?                   @db.UnsignedInt
  modified           DateTime?              @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  modifiedBy         Int?                   @db.UnsignedInt
  courseStartDate    DateTime?              @db.DateTime(0)
  courseEndDate      DateTime?              @db.DateTime(0)

  @@index([accountId], map: "accountId")
  @@index([createdBy], map: "createdBy")
  @@index([modifiedBy], map: "modifiedBy")
}

model accountCustomPageResource {
  pkId              Int    @id @default(autoincrement()) @db.UnsignedInt
  accountId         Int    @db.UnsignedInt
  accountResourceId Int    @db.UnsignedInt
  mediaHash         String @db.VarChar(20)

  @@unique([accountResourceId, mediaHash], map: "accountResourceId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountCustomVideoResource {
  id                            Int       @id @default(autoincrement()) @db.UnsignedInt
  accountId                     Int       @db.UnsignedInt
  accountResourceId             Int       @db.UnsignedInt
  leadGenerationForm            Boolean   @default(true)
  embeddedVideo                 String?   @db.MediumText
  embedOptions                  Boolean?  @default(false)
  interactiveVideoId            String?   @db.VarChar(255)
  webinarVideo                  Boolean   @default(false)
  webinarVideoEventType         Boolean   @default(false)
  webinarVideoTimeZone          Int?
  webinarVideoDailyTime         DateTime? @db.Time(0)
  webinarVideoWeeklyDay         Boolean?  @default(false)
  webinarVideoWeeklyTime        DateTime? @db.Time(0)
  webinarVideoMonthlyDate       Int       @db.TinyInt
  webinarVideoMonthlyTime       DateTime? @db.Time(0)
  webinarVideoDateTime          DateTime? @default(now()) @db.DateTime(0)
  webinarVideoCountdownStyle    Boolean?
  actionButtonLabel1            String?   @db.MediumText
  actionButtonLink1             String?   @db.MediumText
  actionButtonLabel2            String?   @db.MediumText
  actionButtonLink2             String?   @db.MediumText
  actionButtonLabel3            String?   @db.MediumText
  actionButtonLink3             String?   @db.MediumText
  actionButtonLink3Timing       Boolean   @default(false)
  actionButtonLink3TimingMinute Int?      @default(0)
  actionButtonLink2Timing       Boolean   @default(false)
  actionButtonLink2TimingMinute Int?      @default(0)
  actionButtonLink1Timing       Boolean   @default(false)
  actionButtonLink1TimingMinute Int?      @default(0)
  textHead                      String?   @db.MediumText
  textTop                       String?   @db.MediumText
  textBottom                    String?   @db.MediumText
  mediaName                     String?   @db.VarChar(200)
  mediaDuration                 String?   @db.VarChar(100)
  mediaHash                     String?   @db.VarChar(200)
  engagementRate                String?   @db.VarChar(25)
  autoPlay                      Boolean?  @default(false)
  playerControl                 Boolean?  @default(false)
  playlistLoop                  Boolean?  @default(false)
  videoLogo                     String?   @db.VarChar(100)
  videoPageBackgroundColor      String?   @db.VarChar(100)
  companyLogo                   String?   @db.VarChar(100)
  zoomMeetingId                 String?   @db.VarChar(50)
  zoomJoinUrl                   String?   @db.VarChar(100)
  zoomMeetingName               String?   @db.VarChar(200)
  zoomMeetingDuration           String?   @db.VarChar(100)
  zoomType                      Boolean?
  zoomUserId                    Int?      @db.UnsignedInt
  videoBackgroundType           Boolean   @default(false)
  callbackRequestButton         Boolean   @default(true)
  actionButtonLinkOneOpenType   Int?      @default(0) @db.TinyInt
  actionButtonLinkTwoOpenType   Int?      @default(0) @db.TinyInt
  actionButtonLinkThreeOpenType Int?      @default(0) @db.TinyInt
  customColorToUse              Boolean?  @default(false)
  primaryColorVideo             String?   @db.VarChar(20)
  secondaryColorVideo           String?   @db.VarChar(20)
  tertiaryColorVideo            String?   @db.VarChar(20)
  videoHeaderText               String?   @db.Text
  videoDescriptionText          String?   @db.Text
  videoHeadline                 String?   @db.Text
  headerFont                    String?   @db.VarChar(200)
  bodyFont                      String?   @db.VarChar(200)
  showBookMeBtn                 Boolean?  @default(false)
  videoOrientationType          Boolean?  @default(false)

  @@index([accountId], map: "accountId")
  @@index([accountResourceId, accountId], map: "accountResourceId")
}

model accountDefaults {
  id                             Int     @id @default(autoincrement())
  upgradeInformationForFreeUsers String? @db.MediumText
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountEmailLog {
  id                BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  messageId         String   @db.VarChar(250)
  accountId         Int
  recipientId       Int
  recipient         String   @db.VarChar(100)
  recipientType     Int      @default(0) @db.TinyInt
  subject           String   @db.VarChar(200)
  sendDate          DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)
  month             Int?     @default(0) @db.TinyInt
  status            Int?     @db.TinyInt
  eventDate         DateTime @default(now()) @db.Timestamp(0)
  mailType          String?  @db.VarChar(50)
  mailId            Int?
  campaignName      String   @default("") @db.VarChar(255)
  emailSendDay      Int      @default(0) @db.TinyInt
  campaignId        Int?
  contactCreatedBy  Int      @default(0)
  broadcastType     Int      @db.TinyInt
  eventOccurredTime DateTime @default(now()) @db.Timestamp(0)
  clickedLink       String?  @default("") @db.VarChar(512)
  isDelete          Int      @default(0) @db.TinyInt
  hideFromUser      Int      @default(0) @db.TinyInt

  @@index([accountId], map: "accountId")
  @@index([contactCreatedBy], map: "contactCreatedBy")
  @@index([campaignId], map: "emailLog_campaignId")
  @@index([recipientId], map: "recipientId")
}

model accountEmailResource {
  id              Int  @id @default(autoincrement())
  campaignEmailId Int  @default(0)
  campaignId      Int?
  systemEmailId   Int  @default(0)
  resourceId      Int

  @@index([campaignEmailId], map: "campaignEmailId")
  @@index([resourceId], map: "resourceId")
  @@index([systemEmailId], map: "systemEmailId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountEventLog {
  id       Int       @id @default(autoincrement()) @db.UnsignedInt
  eventId  Int       @db.UnsignedInt
  userId   Int?      @db.UnsignedInt
  created  DateTime  @default(now()) @db.Timestamp(0)
  modified DateTime? @default(now()) @db.Timestamp(0)
}

model accountFlags {
  id        Int      @id @default(autoincrement())
  accountId Int
  flagName  String   @db.VarChar(100)
  flagValue Boolean  @default(false)
  created   DateTime @default(now()) @db.Timestamp(0)
  modified  DateTime @default(now()) @db.Timestamp(0)

  @@unique([accountId, flagName], map: "acFlagUnique")
}

model accountFreeUserCampaigns {
  pkId              Int @id @default(autoincrement()) @db.UnsignedInt
  accountId         Int
  accountCampaignId Int

  @@unique([accountCampaignId, accountId], map: "accountCampaignId")
}

model accountFreeUserResources {
  pkId              Int @id @default(autoincrement()) @db.UnsignedInt
  accountId         Int
  accountResourceId Int

  @@unique([accountResourceId, accountId], map: "accountResourceId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountGameLevel {
  id        Int      @id @default(autoincrement())
  accountId Int
  levelName String   @db.VarChar(200)
  levelType Boolean
  subLevel  String?  @db.VarChar(250)
  goal      String   @db.VarChar(250)
  gameStage Int
  isDeleted Boolean  @default(false)
  created   DateTime @default(now()) @db.Timestamp(0)
  updated   DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountGameLevelAssets {
  id                 Int                               @id @default(autoincrement())
  assetId            Int
  accountId          Int
  accountGameLevelId Int
  assetType          accountGameLevelAssets_assetType?
  created            DateTime                          @default(now()) @db.Timestamp(0)
  updated            DateTime                          @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)

  @@index([accountGameLevelId], map: "accountGameLevelId")
  @@index([assetId], map: "assetId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountGroup {
  id                 Int      @id @default(autoincrement())
  groupCode          String?  @db.VarChar(16)
  accountId          Int
  resourceId         Int?     @default(0)
  name               String   @db.VarChar(255)
  description        String   @db.VarChar(128)
  parentGroupId      Int?
  rank               Int?
  options            Boolean?
  subOptions         Boolean?
  goal               String?  @db.VarChar(250)
  autoAssign         Boolean  @default(false)
  created            DateTime @default(now()) @db.Timestamp(0)
  updated            DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  restrictFlag       Boolean  @default(false)
  welcomeVideoOption Boolean  @default(false)
  embeddedVideo      String?  @db.MediumText
  mediaName          String?  @db.VarChar(200)
  mediaDuration      String?  @db.VarChar(100)
  mediaHash          String?  @db.VarChar(200)
  groupLogo          String?  @db.VarChar(100)
  groupBgImage       String?  @db.VarChar(100)

  @@index([accountId], map: "accountId")
  @@index([parentGroupId], map: "parentGroupId")
  @@fulltext([name], map: "name")
}

model accountGroupAlias {
  id             Int      @id @default(autoincrement()) @db.UnsignedInt
  accountGroupId Int      @db.UnsignedInt
  aliasGroupCode String   @db.VarChar(50)
  created        DateTime @default(now()) @db.Timestamp(0)
  createdBy      Int      @db.UnsignedInt
  modified       DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  modifiedBy     Int?     @db.UnsignedInt

  @@index([accountGroupId], map: "accountGroupId")
  @@index([aliasGroupCode], map: "aliasGroupCode")
}

model accountGroupLeaderGroups {
  pkId           Int      @id @default(autoincrement()) @db.UnsignedInt
  accountUserId  Int      @db.UnsignedInt
  accountGroupId Int      @db.UnsignedInt
  created        DateTime @default(now()) @db.Timestamp(0)

  @@unique([accountUserId, accountGroupId], map: "accountUserId")
  @@index([accountGroupId], map: "accountGroupId")
}

model accountIncentiveCampaign {
  pkId                      Int @id @default(autoincrement()) @db.UnsignedInt
  accountIncentiveProgramId Int @db.UnsignedInt
  accountCampaignId         Int @db.UnsignedInt

  @@unique([accountIncentiveProgramId, accountCampaignId], map: "accountIncentiveProgramId")
}

model accountIncentiveGroup {
  pkId                      Int @id @default(autoincrement()) @db.UnsignedInt
  accountIncentiveProgramId Int @db.UnsignedInt
  accountGroupId            Int @db.UnsignedInt

  @@unique([accountIncentiveProgramId, accountGroupId], map: "accountIncentiveProgramId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountIncentiveProgram {
  id                   Int                                 @id @default(autoincrement())
  accountId            Int
  activityToInclude    Boolean?
  name                 String                              @db.VarChar(255)
  accountAwardTypeId   Int
  startDate            DateTime                            @default(now()) @db.Timestamp(0)
  endDate              DateTime?                           @db.Timestamp(0)
  leadsGenerated       Int?
  award                String?                             @db.VarChar(255)
  awardFullDescription String?                             @db.Text
  goal                 Int?
  topPercentage        Int?                                @db.TinyInt
  subject              String?                             @db.VarChar(255)
  emailContent         String?                             @db.MediumText
  from                 String?                             @db.VarChar(100)
  includeUser          String?                             @default("all") @db.VarChar(10)
  isComplete           Int?
  topUser              Int?
  userNumber           Int?
  created              DateTime                            @default(now()) @db.Timestamp(0)
  modified             DateTime                            @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  allLevelSelected     Boolean                             @default(false)
  repeatType           accountIncentiveProgram_repeatType?
  repeatDone           String?                             @db.VarChar(10)
  timeZoneId           Int                                 @default(97) @db.SmallInt
  emailIds             String?                             @db.Text
  resCompletion        Int?
  resourceOption       String?                             @db.Text
  notifyTime           Boolean?
  emailSentStatus      Boolean                             @default(false)
  includeInactiveRes   Int                                 @default(0) @db.TinyInt

  @@index([accountAwardTypeId], map: "accountAwardTypeId")
  @@index([accountId], map: "accountId")
  @@index([timeZoneId], map: "aip_timeZoneId")
  @@fulltext([name], map: "name")
}

model accountIncentiveResources {
  id          Int  @id @default(autoincrement())
  incentiveId Int?
  resourceId  Int?

  @@index([incentiveId, resourceId], map: "incentiveId")
}

model accountLandingPage {
  id                 Int     @id @default(autoincrement())
  accountId          Int
  accountUserId      Int
  leadGenerationForm Int     @default(1)
  personalPhoto      String? @db.VarChar(100)
  embeddedVideo      String? @db.MediumText
  actionButtonLabel1 String? @db.MediumText
  actionButtonLink1  String? @db.MediumText
  actionButtonLabel2 String? @db.MediumText
  actionButtonLink2  String? @db.MediumText
  actionButtonLabel3 String? @db.MediumText
  actionButtonLink3  String? @db.MediumText

  @@unique([accountUserId, accountId], map: "accountUserId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountLeadSource {
  id         Int      @id @default(autoincrement()) @db.UnsignedInt
  accountId  Int      @db.UnsignedInt
  name       String   @default("") @db.VarChar(255)
  linkText   String   @default("") @db.VarChar(255)
  link       String   @db.Text
  created    DateTime @default(now()) @db.Timestamp(0)
  createdBy  Int      @db.UnsignedInt
  modified   DateTime @default(now()) @db.Timestamp(0)
  modifiedBy Int      @db.UnsignedInt

  @@index([accountId], map: "als_accountId")
}

model accountLeadSourceGroup {
  accountLeadSourceId Int      @db.UnsignedInt
  accountGroupId      Int      @db.UnsignedInt
  created             DateTime @default(now()) @db.Timestamp(0)

  @@id([accountLeadSourceId, accountGroupId])
  @@index([accountGroupId], map: "alsg_accountGroupId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountLevel {
  id                Int                 @id @default(autoincrement())
  name              String?             @db.VarChar(255)
  description       String?             @db.VarChar(255)
  pricePerMonth     Float               @default(0) @db.Float
  pricePerUser      Float               @default(0) @db.Float
  callForPricing    Boolean             @default(false)
  maxUsers          Int                 @default(0)
  maxContacts       Int                 @default(0)
  maxCampaigns      Int                 @default(0)
  maxCampaignEmails Int                 @default(0)
  status            accountLevel_status @default(VALUE_1)
  dateCreated       DateTime            @default(now()) @db.Timestamp(0)
  createdBy         Int
  dateModified      DateTime            @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  modifiedBy        Int?

  @@index([createdBy], map: "createdBy")
  @@index([modifiedBy], map: "modifiedBy")
  @@index([status], map: "status")
}

model accountLinkTracking {
  pkId        Int      @id @default(autoincrement()) @db.UnsignedInt
  userId      Int
  contactId   Int
  campaignId  Int
  link        String   @db.VarChar(1000)
  clickedDate DateTime @db.DateTime(0)
  contactHash String?  @db.Char(128)

  @@index([contactHash], map: "con_hash")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountLogs {
  id                               Int      @id @default(autoincrement())
  accountId                        Int
  rfAdmin                          Boolean  @default(false)
  createdVsUpgraded                String?  @db.MediumText
  usersLastLoginWithinXdaysVsYdays String?  @db.MediumText
  weeklyOptInRate                  String?  @db.MediumText
  created                          DateTime @db.DateTime(0)
  updated                          DateTime @db.DateTime(0)

  @@unique([accountId, rfAdmin], map: "createdVsUpgraded")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountMailLog {
  id                Int                   @id @default(autoincrement())
  messageId         String                @db.VarChar(250)
  accountId         Int
  recipientId       Int
  recipient         String                @db.VarChar(100)
  recipientType     Int                   @default(0) @db.TinyInt
  subject           String                @db.VarChar(300)
  sendDate          DateTime              @db.DateTime(0)
  month             Int?                  @default(0) @db.TinyInt
  status            accountMailLog_status
  eventDate         DateTime              @default(now()) @db.DateTime(0)
  mailType          String?               @db.VarChar(50)
  mailId            Int?
  campaignId        Int?
  contactCreatedBy  Int                   @default(0)
  campaignName      String                @default("") @db.VarChar(255)
  emailSendDay      Int                   @default(0) @db.TinyInt
  broadcastType     Int?                  @db.TinyInt
  eventOccurredTime DateTime?             @default(now()) @db.Timestamp(0)
  clickedLink       String?               @default("") @db.VarChar(512)
  isDelete          Boolean               @default(false)
  flag              Int                   @default(0)

  @@index([subject(length: 191)], map: "Subject")
  @@index([mailId], map: "accountMailLog_mailId_index")
  @@index([mailType], map: "accountMailLog_mailType_index")
  @@index([campaignId], map: "campaignId")
  @@index([contactCreatedBy], map: "contactCreatedBy")
  @@index([isDelete], map: "isDelete")
  @@index([messageId(length: 191)], map: "messageId")
  @@index([recipient], map: "recipient")
  @@index([recipientId], map: "recipientId")
  @@index([recipientType], map: "recipientType")
  @@fulltext([campaignName], map: "campaignName")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountMailLogCronUpdate {
  id         Boolean  @id
  mailType   Boolean
  lastUpdate DateTime @default(now()) @db.Timestamp(0)

  @@index([mailType], map: "mailType")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountNoOfPaidFreeUser {
  id           Int       @id @default(autoincrement())
  accountId    Int       @db.UnsignedInt
  noOfPaidUser Int       @default(0)
  noOfFreeUser Int       @default(0)
  created      DateTime  @default(now()) @db.Timestamp(0)
  modified     DateTime? @db.Timestamp(0)

  @@index([accountId], map: "accountId")
}

model accountPostingKeys {
  id         Int      @id @default(autoincrement()) @db.UnsignedInt
  postingKey String?  @db.VarChar(255)
  createdAt  DateTime @default(now()) @db.Timestamp(0)
  note       String?  @db.VarChar(200)
  accountId  Int?     @default(0) @db.UnsignedInt

  @@index([postingKey], map: "postingKey")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountPreference {
  id                                        Int                             @unique(map: "id") @default(autoincrement())
  accountId                                 Int                             @id
  enableTwitterUrl                          Boolean?                        @default(false)
  enableFacebookUrl                         Boolean?                        @default(false)
  enableLinkedinUrl                         Boolean?                        @default(false)
  enableInstagramUrl                        Boolean?                        @default(false)
  enableAdditionalNotificationEmail         Boolean?                        @default(false)
  enableRepId1                              Boolean?                        @default(false)
  enableRepId2                              accountPreference_enableRepId2? @default(VALUE_0)
  enableRepId3                              Boolean?                        @default(false)
  enableRepId4                              Boolean?                        @default(false)
  enableRepId5                              Boolean?                        @default(false)
  enableRepId6                              Boolean?                        @default(false)
  enableRepId7                              Boolean?                        @default(false)
  enableRepId8                              Boolean?                        @default(false)
  enableTwitterUrlRequired                  Int?                            @default(0) @db.TinyInt
  enableFacebookUrlRequired                 Int?                            @default(0) @db.TinyInt
  enableLinkedinUrlRequired                 Boolean?                        @default(false)
  enableInstagramUrlRequired                Boolean?                        @default(false)
  enableAdditionalNotificationEmailRequired Int?                            @default(0) @db.TinyInt
  enableRepId1Required                      Int?                            @default(0) @db.TinyInt
  enableRepId2Required                      Int?                            @default(0) @db.TinyInt
  enableRepId3Required                      Boolean?                        @default(false)
  enableRepId4Required                      Boolean?                        @default(false)
  enableRepId5Required                      Boolean?                        @default(false)
  enableRepId6Required                      Boolean?                        @default(false)
  enableRepId7Required                      Boolean?                        @default(false)
  enableRepId8Required                      Boolean?                        @default(false)
  dateCreated                               DateTime                        @default(now()) @db.Timestamp(0)
  createdBy                                 Int
  dateModified                              DateTime                        @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  modifiedBy                                Int?
  enableRepId1ShowSignup                    Boolean?                        @default(true)
  enableRepId2ShowSignup                    Boolean?                        @default(false)
  enableAddNotifEmailShowSignup             Boolean?                        @default(false)
  enableCustomBookingLink                   Boolean?                        @default(true)
  enableCustomBookingLinkRequired           Boolean?                        @default(false)
  enableTikTokUrl                           Boolean?                        @default(false)
  enableWhatsAppUrl                         Boolean?                        @default(false)
  enableTikTokUrlRequired                   Boolean?                        @default(false)
  enableWhatsAppUrlRequired                 Boolean?                        @default(false)
}

model accountPromoContentType {
  id    Int      @id @default(autoincrement())
  name  String?  @db.VarChar(255)
  order Boolean?
}

model accountRefund {
  id                           Int      @id @default(autoincrement())
  refundTransactionId          String?  @db.VarChar(100)
  origTransactionId            String   @db.VarChar(100)
  authorizeProfileToken        String?  @db.VarChar(100)
  authorizePaymentProfileToken String?  @db.VarChar(100)
  refundAmount                 Float    @db.Float
  method                       String?  @db.VarChar(100)
  approved                     String?  @db.Text
  declined                     Boolean?
  error                        Boolean?
  held                         Boolean?
  transactionType              String?  @db.VarChar(100)
  created                      DateTime @default(now()) @db.Timestamp(0)
  modified                     DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
}

model accountResource {
  id          Int     @id @default(autoincrement())
  name        String  @unique(map: "name") @default("") @db.VarChar(100)
  description String? @db.VarChar(100)
  parent      String? @db.VarChar(150)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountResourceActivityRawLog {
  pkId               Int      @id @default(autoincrement()) @db.UnsignedInt
  resourceId         Int?     @db.UnsignedInt
  userId             Int?     @db.UnsignedInt
  contactIdOrHash    String?  @default("") @db.VarChar(255)
  encRemoteAddr      String?  @default("") @db.VarChar(255)
  redirectUrl        String?  @default("") @db.VarChar(255)
  requestUri         String?  @default("") @db.VarChar(255)
  httpUserAgent      String?  @default("") @db.VarChar(1000)
  httpXRequestedWith String?  @default("") @db.VarChar(100)
  httpAcceptLanguage String?  @default("") @db.VarChar(100)
  httpReferer        String?  @default("") @db.VarChar(250)
  cookieSessionId    String?  @default("") @db.VarChar(250)
  requestTimeFloat   String?  @default("") @db.VarChar(20)
  isBotRequest       Boolean? @default(false)
  info               String?  @db.Text
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountResourceCategory {
  id                 Int      @id @default(autoincrement())
  accountId          Int
  parentId           Int      @default(0)
  isPersonalCategory Boolean  @default(false)
  name               String   @db.VarChar(255)
  description        String?  @db.MediumText
  priority           Int      @default(0)
  created            DateTime @default(now()) @db.Timestamp(0)
  createdBy          Int
  modified           DateTime @default(now()) @db.Timestamp(0)
  modifiedBy         Int?

  @@index([accountId], map: "accountId")
  @@fulltext([name], map: "name")
}

model accountResourceGroup {
  pkId               Int @id @default(autoincrement()) @db.UnsignedInt
  accountResourcesId Int
  accountGroupId     Int

  @@unique([accountResourcesId, accountGroupId], map: "accountResourcesId")
  @@index([accountGroupId], map: "accountGroupId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountResourceLog {
  accountId            Int  @db.UnsignedInt
  roleId               Int  @db.UnsignedTinyInt
  userId               Int  @default(0) @db.UnsignedInt
  resourceId           Int  @db.UnsignedInt
  sentViaWebCount      Int? @default(0) @db.UnsignedInt
  sentViaMobileCount   Int? @default(0) @db.UnsignedInt
  sentViaCampaignCount Int? @default(0) @db.UnsignedInt
  clicked              Int? @default(0) @db.UnsignedInt
  opened               Int? @default(0) @db.UnsignedInt
  viewedCount          Int? @default(0) @db.UnsignedInt
  contactsCaptured     Int? @default(0) @db.UnsignedInt

  @@id([accountId, roleId, userId, resourceId])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountResourceStats {
  accountId            Int  @db.UnsignedInt
  roleId               Int  @db.UnsignedTinyInt
  userId               Int  @default(0) @db.UnsignedInt
  resourceId           Int  @db.UnsignedInt
  sentViaWebCount      Int? @default(0) @db.UnsignedInt
  sentViaMobileCount   Int? @default(0) @db.UnsignedInt
  sentViaCampaignCount Int? @default(0) @db.UnsignedInt
  clicked              Int? @default(0) @db.UnsignedInt
  opened               Int? @default(0) @db.UnsignedInt
  viewedCount          Int? @default(0) @db.UnsignedInt
  contactsCaptured     Int? @default(0) @db.UnsignedInt

  @@id([accountId, roleId, userId, resourceId])
}

model accountResourceType {
  id        Int     @id @default(autoincrement())
  name      String  @db.VarChar(100)
  typeImage String? @db.VarChar(200)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountResources {
  id                        Int                          @id @default(autoincrement())
  accountId                 Int
  accountResourceTypeId     Int
  accountResourceCategoryId Int?
  name                      String                       @db.VarChar(255)
  description               String?                      @db.LongText
  externalDescription       String?                      @db.LongText
  content                   String?                      @db.LongText
  link                      String?                      @db.VarChar(1000)
  fileSavedName             String?                      @db.VarChar(255)
  fileOriginalName          String?                      @db.VarChar(255)
  showInApp                 accountResources_showInApp   @default(VALUE_0)
  overrideExistingUsers     Boolean                      @default(false)
  status                    accountResources_status      @default(VALUE_1)
  freeResource              Boolean                      @default(false)
  allLevelSelected          Boolean                      @default(false)
  allGroupSelected          Boolean                      @default(false)
  isPersonalResource        Int?                         @default(0) @db.TinyInt
  useCompanyResource        Boolean                      @default(false)
  companyResourceId         Int?                         @db.UnsignedInt
  isPublished               accountResources_isPublished @default(VALUE_0)
  noOfTimeFormCompleted     Int                          @default(0)
  thumbnailImgUrl           String?                      @db.VarChar(200)
  defaultCampaignId         Int?
  longMessage               String?                      @db.MediumText
  subject                   String?                      @db.VarChar(255)
  shortMessage              String?                      @db.MediumText
  resourceOrder             Int?                         @default(1000)
  created                   DateTime                     @db.DateTime(0)
  createdBy                 Int
  modified                  DateTime                     @default(now()) @db.Timestamp(0)
  modifiedBy                Int?
  setDateRange              Boolean                      @default(false)
  resStartDate              DateTime?                    @db.DateTime(0)
  resEndDate                DateTime?                    @db.DateTime(0)
  resEndAction              Boolean?
  resAppUpdateProgress      Int?                         @default(0) @db.TinyInt

  @@index([accountId], map: "accountId")
  @@index([accountResourceCategoryId], map: "accountResourceCategoryId")
  @@index([accountResourceTypeId], map: "accountResourceTypeId")
  @@fulltext([name], map: "name")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountRoles {
  id            Int    @id @default(autoincrement())
  name          String @db.VarChar(100)
  description   String @db.VarChar(100)
  roleSortOrder Int?   @db.TinyInt
}

model accountRolesCustom {
  id        Int    @id @default(autoincrement())
  name      String @db.VarChar(100)
  parentId  Int    @default(0)
  accountId Int
}

model accountRolesResources {
  accountRoleId     Int
  accountResourceId Int
  customRoleId      Int

  @@id([accountRoleId, accountResourceId, customRoleId])
  @@index([accountResourceId], map: "accountResourceId")
}

model accountSegment {
  id   Int    @id @default(autoincrement()) @db.UnsignedInt
  name String @db.VarChar(255)
}

model accountStatus {
  id          Int     @id @default(autoincrement())
  name        String? @db.VarChar(255)
  description String? @db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountStreak {
  id               Int      @id @default(autoincrement())
  accountId        Int
  type             Boolean
  count            Int
  duration         Boolean
  status           Boolean  @default(true)
  allGroupSelected Boolean  @default(false)
  created          DateTime @db.DateTime(0)
  createdBy        Int
  modified         DateTime @default(now()) @db.Timestamp(0)
  modifiedBy       Int?

  @@index([accountId], map: "accountId")
  @@index([type], map: "type")
}

model accountStreakGroup {
  accountStreakId Int
  accountGroupId  Int

  @@unique([accountStreakId, accountGroupId], map: "accountResourcesId")
  @@index([accountGroupId], map: "accountGroupId")
  @@index([accountStreakId], map: "accountStreakId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountSubscriptionPlans {
  id                  Int      @id @default(autoincrement())
  accountId           Int      @unique(map: "accountId")
  type                Boolean? @default(false)
  interval            Boolean? @default(true)
  basePrice           Decimal? @default(0.00) @db.Decimal(9, 2)
  basePricePlanId     String?  @db.VarChar(255)
  defMonthlyPlanId    String?  @default("users-def-monthly-plan") @db.VarChar(30)
  defYearlyPlanId     String?  @default("users-def-monthly-plan") @db.VarChar(30)
  tieredPricingPlanId Int?     @default(0)
  modifiedTime        DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  modifiedBy          Int?

  @@index([defMonthlyPlanId], map: "defMonthlyPlanId")
  @@index([defYearlyPlanId], map: "defYearlyPlanId")
}

model accountSystemEmailType {
  id   Int    @id @default(autoincrement()) @db.UnsignedTinyInt
  name String @db.VarChar(100)

  @@fulltext([name], map: "name")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountSystemEmailUserEmail {
  id            Int                                    @id @unique(map: "id") @default(autoincrement())
  accountId     Int
  type          Int?                                   @db.TinyInt
  systemEmailId Int
  userId        Int
  sendOnDate    DateTime                               @db.Date
  sendOnTime    DateTime?                              @default(dbgenerated("'00:00:00'")) @db.Time(0)
  sendStatus    accountSystemEmailUserEmail_sendStatus @default(VALUE_0)
  created       DateTime                               @default(now()) @db.Timestamp(0)

  @@index([systemEmailId], map: "accountsystememailuseremail_systememailid_index")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountSystemEmails {
  id                Int                                 @id @default(autoincrement())
  accountId         Int
  type              Int                                 @db.TinyInt
  description       String?                             @db.MediumText
  fromEmail         String?                             @db.VarChar(255)
  subject           String?                             @db.VarChar(255)
  htmlBody          String?                             @db.MediumText
  textBody          String?                             @db.MediumText
  status            accountSystemEmails_status          @default(VALUE_1)
  isOrFromDefault   accountSystemEmails_isOrFromDefault @default(VALUE_0)
  days              Int?                                @db.SmallInt
  time              DateTime?                           @default(dbgenerated("'00:00:00'")) @db.Time(0)
  created           DateTime                            @default(now()) @db.Timestamp(0)
  createdBy         Int
  modified          DateTime                            @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  modifiedBy        Int?
  totalSent         Int?                                @default(0) @db.UnsignedMediumInt
  totalDelivered    Int?                                @default(0) @db.UnsignedMediumInt
  totalOpened       Int?                                @default(0) @db.UnsignedMediumInt
  totalClicked      Int?                                @default(0) @db.UnsignedMediumInt
  totalBounced      Int?                                @default(0) @db.UnsignedMediumInt
  totalComplained   Int?                                @default(0) @db.UnsignedMediumInt
  totalUnsubscribed Int?                                @default(0) @db.UnsignedMediumInt

  @@index([accountId], map: "accountId")
  @@fulltext([description], map: "description")
  @@fulltext([subject], map: "subject")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountTasks {
  id          Int       @id @default(autoincrement()) @db.UnsignedInt
  accountId   Int       @db.UnsignedInt
  userId      Int       @db.UnsignedInt
  contactId   Int?      @db.UnsignedInt
  eventId     Int?
  title       String    @db.VarChar(100)
  description String    @db.Text
  taskTime    DateTime  @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)
  isAllDay    Boolean   @default(false)
  remindMe    Boolean?
  taskType    Int       @db.TinyInt
  isCompleted Boolean   @default(false)
  createdBy   Int       @db.UnsignedInt
  createdOn   DateTime  @default(now()) @db.Timestamp(0)
  modifiedOn  DateTime  @default(now()) @db.Timestamp(0)
  completedOn DateTime? @db.Timestamp(0)

  @@index([accountId], map: "accountId")
  @@index([contactId], map: "contactId")
  @@index([userId], map: "userId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountTeammate {
  id                                                         BigInt      @id @default(autoincrement())
  requestByUserId                                            BigInt
  requestedToUserId                                          BigInt
  accountId                                                  Int
  status                                                     Int         @default(0) @db.TinyInt
  sentOn                                                     DateTime    @default(now()) @db.DateTime(0)
  acceptedOn                                                 DateTime?   @db.DateTime(0)
  isNotificationViewed                                       Int         @default(0) @db.TinyInt
  accountUser_accountTeammate_requestByUserIdToaccountUser   accountUser @relation("accountTeammate_requestByUserIdToaccountUser", fields: [requestByUserId], references: [id], onUpdate: Restrict, map: "accountTeammate_accountUser_id_fk")
  accountUser_accountTeammate_requestedToUserIdToaccountUser accountUser @relation("accountTeammate_requestedToUserIdToaccountUser", fields: [requestedToUserId], references: [id], onUpdate: Restrict, map: "accountTeammate_accountUser_id_fk_2")

  @@index([accountId], map: "accountTeammate_accountId_index")
  @@index([requestByUserId], map: "accountTeammate_accountUser_id_fk")
  @@index([requestedToUserId], map: "accountTeammate_accountUser_id_fk_2")
  @@index([isNotificationViewed], map: "accountTeammate_isNotificationViewed_index")
  @@index([status], map: "accountTeammate_status_index")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountTeammates {
  id                   Int       @id @default(autoincrement()) @db.UnsignedInt
  reqByUserId          Int       @db.UnsignedInt
  reqToUserId          Int       @db.UnsignedInt
  accountId            Int       @db.UnsignedInt
  status               Boolean   @default(false)
  sentOn               DateTime  @default(now()) @db.DateTime(0)
  acceptedOn           DateTime? @db.DateTime(0)
  statusUpdatedOn      DateTime? @db.DateTime(0)
  isNotificationViewed Boolean   @default(false)

  @@index([accountId], map: "accountTeammates_accountId_index")
  @@index([isNotificationViewed], map: "accountTeammates_isNotificationViewed_index")
  @@index([reqByUserId], map: "accountTeammates_reqByUserId_index")
  @@index([reqToUserId], map: "accountTeammates_reqToUserId_index")
  @@index([status], map: "accountTeammates_status_index")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountTeammatesMessageLog {
  id                   Int       @id @default(autoincrement()) @db.UnsignedInt
  teammateId           Int       @db.UnsignedInt
  reqByUserId          Int       @db.UnsignedInt
  reqToUserId          Int       @db.UnsignedInt
  accountId            Int       @db.UnsignedInt
  type                 Boolean?  @default(false)
  reqMessage           String?   @db.VarChar(255)
  sentOn               DateTime  @default(now()) @db.DateTime(0)
  isNotificationViewed Boolean   @default(false)
  resMessage           String?   @db.VarChar(255)
  repliedOn            DateTime? @db.DateTime(0)

  @@index([accountId], map: "accountTeammatesMessageLog_accountId_index")
  @@index([reqByUserId], map: "accountTeammatesMessageLog_reqByUserId_index")
  @@index([reqToUserId], map: "accountTeammatesMessageLog_reqToUserId_index")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountTimeZone {
  id           Int     @id @default(autoincrement()) @db.UnsignedInt
  timezone     String? @db.VarChar(255)
  abbreviation String? @db.VarChar(100)
  tzOffSet     String? @db.Char(255)
  dstFlag      Int?    @default(0) @db.TinyInt
  shortName    String? @db.VarChar(100)
  priority     Int?    @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountTransactions {
  id                 Int       @id @default(autoincrement()) @db.UnsignedInt
  accountId          Int       @db.UnsignedInt
  userId             Int?      @db.UnsignedInt
  eventId            String?   @unique(map: "eventId") @db.VarChar(255)
  eventType          String    @db.VarChar(100)
  stripeCustomerId   String    @db.VarChar(255)
  subscriptionId     String?   @db.VarChar(255)
  subscriptionPlanId String?   @db.VarChar(255)
  invoiceNumber      String?   @db.VarChar(255)
  paymentFor         String?   @db.VarChar(20)
  last4CardNumber    String    @db.VarChar(4)
  amount             Decimal?  @default(0.00) @db.Decimal(9, 2)
  chargeId           String?   @db.VarChar(255)
  paymentStatus      Boolean   @default(false)
  refundId           String?   @db.VarChar(255)
  refundAmount       Decimal?  @default(0.00) @db.Decimal(9, 2)
  refundCreated      DateTime? @default(now()) @db.Timestamp(0)
  created            DateTime  @default(now()) @db.Timestamp(0)
  modified           DateTime? @default(now()) @db.Timestamp(0)

  @@index([chargeId], map: "chargeId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountUrlShortener {
  id          Int      @id @default(autoincrement())
  longUrl     String?  @db.MediumText
  shortUrl    String   @db.VarChar(200)
  dateCreated DateTime @default(now()) @db.Timestamp(0)
  counter     Int      @default(0)

  @@index([shortUrl(length: 191)], map: "shortUrl")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountUser {
  id                                                             BigInt                                @id @default(autoincrement())
  accountId                                                      Int
  resourceId                                                     Int?                                  @default(0)
  email                                                          String?                               @db.VarChar(255)
  new                                                            Int?                                  @default(1)
  firstName                                                      String?                               @db.VarChar(45)
  lastName                                                       String?                               @db.VarChar(45)
  personalCode                                                   String?                               @db.VarChar(50)
  title                                                          String?                               @db.VarChar(100)
  phoneNumber                                                    String?                               @db.VarChar(20)
  created                                                        DateTime                              @default(now()) @db.Timestamp(0)
  createdBy                                                      Int?
  createdFrom                                                    String?                               @db.VarChar(10)
  modified                                                       DateTime?                             @default(now()) @db.Timestamp(0)
  modifiedBy                                                     Int?
  password                                                       String                                @db.VarChar(128)
  accessToken                                                    String?                               @db.Text
  accessTokenExpiresOn                                           DateTime?                             @db.DateTime(0)
  salt                                                           String                                @db.Text
  forceChange                                                    Int                                   @default(1) @db.TinyInt
  passwordResetSendTime                                          DateTime?                             @db.Timestamp(0)
  profileImage                                                   String?                               @db.VarChar(45)
  address                                                        String?                               @db.VarChar(255)
  suite                                                          String?                               @db.VarChar(255)
  city                                                           String?                               @db.VarChar(255)
  stateId                                                        Int?                                  @db.UnsignedInt
  countryId                                                      Int?                                  @db.UnsignedInt
  zip                                                            String?                               @db.VarChar(20)
  roleId                                                         Int                                   @default(3)
  customRoleId                                                   Int?                                  @default(0)
  status                                                         accountUser_status                    @default(VALUE_1)
  autoSuspendStatus                                              Boolean                               @default(false)
  isTrial                                                        Boolean                               @default(false)
  trialExpiresOn                                                 DateTime?                             @db.DateTime(0)
  isCancelPremiumService                                         Boolean?                              @default(false)
  premiumCancelledOn                                             DateTime?                             @default(dbgenerated("'0000-00-00'")) @db.Date
  isDelete                                                       Boolean                               @default(false)
  softDeletedDate                                                DateTime?                             @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)
  isBillingContact                                               Boolean                               @default(false)
  registrationCode                                               String?                               @db.VarChar(255)
  invitationDate                                                 DateTime?                             @db.Date
  lastLogin                                                      DateTime?                             @default(now()) @db.Timestamp(0)
  linkTrackingNotify                                             accountUser_linkTrackingNotify        @default(VALUE_3)
  additionalEmailNotification                                    String?                               @db.VarChar(255)
  repId                                                          String?                               @db.VarChar(50)
  repId2                                                         String?                               @db.VarChar(200)
  customBookingLink                                              String?                               @db.VarChar(255)
  facebookUrl                                                    String?                               @db.VarChar(255)
  twitterUrl                                                     String?                               @db.VarChar(255)
  linkedinUrl                                                    String?                               @db.VarChar(255)
  instagramUrl                                                   String?                               @db.VarChar(255)
  tikTokUrl                                                      String?                               @db.VarChar(255)
  whatsAppUrl                                                    String?                               @db.VarChar(255)
  googleTrackingCode                                             String?                               @db.VarChar(100)
  fbTrackingCode                                                 String?                               @db.VarChar(100)
  fbPageId                                                       String?                               @db.VarChar(100)
  ignoreUserPayment                                              accountUser_ignoreUserPayment         @default(VALUE_0)
  contactRecordsPerPage                                          Int?                                  @db.TinyInt
  resourceRecordsPerPage                                         Int?                                  @db.TinyInt
  userRecordsPerPage                                             Int?                                  @db.TinyInt
  groupRecordsPerPage                                            Int?                                  @db.TinyInt
  campaignRecordsPerPage                                         Int?                                  @db.TinyInt
  broadcastRecordsPerPage                                        Int?                                  @db.TinyInt
  currProgramRecordsPerPage                                      Int?                                  @db.TinyInt
  pastProgramRecordsPerPage                                      Int?                                  @db.TinyInt
  systemEmailsRecordsPerPage                                     Int?                                  @db.TinyInt
  newUserEmailsRecordsPerPage                                    Int?                                  @db.TinyInt
  contactDefSort                                                 String?                               @db.VarChar(20)
  resetPassHash                                                  String?                               @default("") @db.VarChar(100)
  webViewToken                                                   String?                               @db.Text
  realmToken                                                     String?                               @db.Text
  userToken                                                      String?                               @db.Text
  exportContacts                                                 Boolean                               @default(false)
  exportQuery                                                    String?                               @db.Text
  contactIdsOnlyQuery                                            String?                               @db.Text
  exportType                                                     Int?                                  @default(1)
  totalExportRecords                                             Int?                                  @default(0)
  sqsMessageId                                                   String?                               @db.VarChar(200)
  payThroughAuthNet                                              Int                                   @default(0) @db.TinyInt
  rewardNotifications                                            accountUser_rewardNotifications?      @default(VALUE_3)
  emailsFromAdministrators                                       accountUser_emailsFromAdministrators? @default(VALUE_3)
  eventNotifications                                             accountUser_eventNotifications?       @default(VALUE_1)
  contactOptInStatus                                             Boolean                               @default(false)
  myWeeklyStats                                                  Boolean?                              @default(true)
  failedLoginCount                                               Int?                                  @default(0)
  isAccountLocked                                                Boolean?                              @default(false)
  accountLockedFrom                                              DateTime?                             @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  userTimezone                                                   Int?
  isYBRView                                                      Boolean?                              @default(false)
  twilioCountry                                                  String?                               @db.VarChar(10)
  newRealmUser                                                   Int?                                  @db.TinyInt
  realmUsersIdentity                                             String?                               @db.VarChar(100)
  rlmPermissionCount                                             Boolean?                              @default(false)
  resourceStatsFromDate                                          DateTime?                             @db.DateTime(0)
  resourceStatsToDate                                            DateTime?                             @db.DateTime(0)
  resourceStatsLastUpdatedDate                                   DateTime?                             @db.Date
  wcVideoShown                                                   Boolean?                              @default(false)
  language                                                       String?                               @default("en") @db.VarChar(50)
  countryPhoneCode                                               String?                               @db.VarChar(100)
  fiveContactTransferredUsers                                    String?                               @db.VarChar(100)
  companyName                                                    String?                               @db.VarChar(100)
  subscriptionStatus                                             accountUser_subscriptionStatus?       @default(VALUE_1)
  churnRate                                                      Float?
  churnUpdatedAt                                                 DateTime?                             @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)
  repId3                                                         String?                               @db.VarChar(255)
  repId4                                                         String?                               @db.VarChar(255)
  repId5                                                         String?                               @db.VarChar(255)
  repId6                                                         String?                               @db.VarChar(255)
  repId7                                                         String?                               @db.VarChar(255)
  repId8                                                         String?                               @db.VarChar(255)
  contactBroadcastAbility                                        Int?                                  @default(0) @db.TinyInt
  userAccessLevel                                                accountUser_userAccessLevel           @default(VALUE_0)
  disabled                                                       Int                                   @default(0) @db.UnsignedTinyInt
  iCueUserId                                                     BigInt?                               @db.UnsignedBigInt
  accountTeammate_accountTeammate_requestByUserIdToaccountUser   accountTeammate[]                     @relation("accountTeammate_requestByUserIdToaccountUser")
  accountTeammate_accountTeammate_requestedToUserIdToaccountUser accountTeammate[]                     @relation("accountTeammate_requestedToUserIdToaccountUser")

  @@unique([accountId, email], map: "accountUser_accountId_email_uindex")
  @@index([accountId], map: "accountId")
  @@index([email], map: "accountUser_email")
  @@index([createdBy], map: "createdBy")
  @@index([modifiedBy], map: "modifiedBy")
  @@index([personalCode], map: "personalCode")
  @@index([sqsMessageId], map: "sqsMessageId")
  @@index([stateId], map: "stateId")
  @@fulltext([firstName, lastName], map: "firstName")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountUserActivity {
  id              Int       @id @default(autoincrement()) @db.UnsignedInt
  accountId       Int
  accountUserId   Int
  lastNewActivity DateTime? @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  lastActivity    String?   @default("") @db.VarChar(100)
  accessedFrom    Int?      @default(1)
}

model accountUserAppCampaign {
  pkId              Int      @id @default(autoincrement()) @db.UnsignedInt
  accountUserId     Int
  accountCampaignId Int
  showInApp         Boolean  @default(true)
  created           DateTime @default(now()) @db.Timestamp(0)

  @@unique([accountUserId, accountCampaignId], map: "accountUserId")
}

model accountUserAppResource {
  pkId              Int      @id @default(autoincrement()) @db.UnsignedInt
  accountUserId     Int
  accountResourceId Int
  showInApp         Boolean  @default(true)
  created           DateTime @default(now()) @db.Timestamp(0)

  @@unique([accountUserId, accountResourceId], map: "accountUserId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountUserBillingInfo {
  id                      Int       @id @default(autoincrement()) @db.UnsignedInt
  accountId               Int       @db.UnsignedInt
  userId                  Int       @db.UnsignedInt
  isForCompany            Boolean   @default(false)
  stripeCustomerId        String    @unique(map: "stripeToken") @db.VarChar(255)
  lastChargeId            String?   @db.VarChar(255)
  name                    String    @db.VarChar(100)
  email                   String    @db.VarChar(100)
  zip                     String    @db.VarChar(20)
  expMonth                String    @db.VarChar(2)
  expYear                 String?   @db.VarChar(4)
  last4                   String?   @db.VarChar(4)
  brand                   String?   @db.VarChar(20)
  funding                 String?   @db.VarChar(20)
  isSubscriptionActive    Boolean   @default(false)
  subscriptionId          String?   @db.VarChar(255)
  subscriptionPlanId      String?   @db.VarChar(255)
  planInterval            Boolean?  @default(true)
  subscriptionAmount      Decimal   @default(0.00) @db.Decimal(6, 2)
  subscriptionCreated     DateTime? @db.Timestamp(0)
  subPaymentStartDate     DateTime  @db.Date
  lastPaymentDoneOn       DateTime  @db.Date
  lastPaidAmount          Decimal   @default(0.00) @db.Decimal(9, 2)
  nextPaymentOn           DateTime? @db.Date
  failureCount            Boolean?  @default(false)
  inActiveOrIgnoreBilling Boolean?  @default(false)
  created                 DateTime  @default(now()) @db.Timestamp(0)
  createdBy               Int?      @db.UnsignedInt
  modified                DateTime  @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  modifiedBy              Int?      @db.UnsignedInt
  migratedToChargify      Int?      @default(0) @db.TinyInt
  productId               String?   @db.VarChar(45)
  productPricePointId     String?   @db.VarChar(45)
  chargifySubscriptionId  String?   @db.VarChar(45)

  @@unique([userId, accountId], map: "userId")
  @@index([accountId], map: "accountId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountUserBusinessCard {
  id                 Int       @id @default(autoincrement()) @db.UnsignedInt
  userId             Int
  name               String?   @db.VarChar(100)
  jobTitle           String?   @db.VarChar(100)
  phone              String?   @db.VarChar(100)
  mobile             String?   @db.VarChar(100)
  email              String?   @db.VarChar(100)
  website            String?   @db.VarChar(100)
  location           String?   @db.VarChar(100)
  facebookUrl        String?   @db.VarChar(100)
  linkedInUrl        String?   @db.VarChar(100)
  twitterUrl         String?   @db.VarChar(100)
  instagramUrl       String?   @db.VarChar(100)
  whatsAppUrl        String?   @db.VarChar(100)
  tikTokUrl          String?   @db.VarChar(100)
  showName           Boolean   @default(true)
  showJobTitle       Boolean   @default(true)
  showPhone          Boolean   @default(true)
  showMobile         Boolean   @default(true)
  showEmail          Boolean   @default(true)
  showWebsite        Boolean   @default(true)
  showLocation       Boolean   @default(true)
  showFacebook       Boolean   @default(true)
  showLinkedIn       Boolean   @default(true)
  showTwitter        Boolean   @default(true)
  showInstagram      Boolean   @default(true)
  showWhatsApp       Boolean   @default(false)
  showTikTok         Boolean   @default(false)
  btnLabel1          String?   @default("") @db.VarChar(255)
  linkedResourceId1  Int       @default(0)
  openInNewTab1      Boolean   @default(true)
  btnLabel2          String?   @default("") @db.VarChar(255)
  linkedResourceId2  Int       @default(0)
  openInNewTab2      Boolean   @default(true)
  btnLabel3          String?   @default("") @db.VarChar(255)
  linkedResourceId3  Int       @default(0)
  openInNewTab3      Boolean   @default(true)
  btnLabel4          String?   @default("") @db.VarChar(255)
  linkedResourceId4  Int       @default(0)
  openInNewTab4      Boolean   @default(true)
  btnLabel5          String?   @default("") @db.VarChar(255)
  linkedResourceId5  Int       @default(0)
  openInNewTab5      Boolean   @default(true)
  btnLabel6          String?   @default("") @db.VarChar(255)
  linkedResourceId6  Int       @default(0)
  openInNewTab6      Boolean   @default(true)
  btnLabel7          String?   @default("") @db.VarChar(255)
  linkedResourceId7  Int       @default(0)
  openInNewTab7      Boolean   @default(true)
  btnLabel8          String?   @default("") @db.VarChar(255)
  linkedResourceId8  Int       @default(0)
  openInNewTab8      Boolean   @default(true)
  btnLabel9          String?   @default("") @db.VarChar(255)
  linkedResourceId9  Int       @default(0)
  openInNewTab9      Boolean   @default(true)
  btnLabel10         String?   @default("") @db.VarChar(255)
  linkedResourceId10 Int       @default(0)
  openInNewTab10     Boolean   @default(true)
  created_at         DateTime? @db.Timestamp(0)
  updated_at         DateTime? @db.Timestamp(0)

  @@index([userId], map: "accountuserbusinesscard_userid_index")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountUserDeleteLog {
  id           Int      @id @default(autoincrement()) @db.UnsignedInt
  userId       Int      @db.UnsignedInt
  userEmail    String   @default("") @db.VarChar(255)
  accountId    Int      @db.UnsignedInt
  rfAdminId    Int      @db.UnsignedInt
  deleteReason String   @db.VarChar(255)
  clientIpAddr String   @db.VarChar(255)
  created      DateTime @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountUserEmailLog {
  id                BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  messageId         String   @db.VarChar(250)
  accountId         Int
  recipientId       Int
  recipient         String   @db.VarChar(100)
  subject           String   @db.VarChar(200)
  sendDate          DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)
  status            Int?     @db.TinyInt
  eventDate         DateTime @default(now()) @db.Timestamp(0)
  mailType          String?  @db.VarChar(50)
  mailId            Int?
  broadcastType     Int      @db.TinyInt
  eventOccurredTime DateTime @default(now()) @db.Timestamp(0)
  clickedLink       String?  @default("") @db.VarChar(512)
  isDelete          Int      @default(0) @db.TinyInt

  @@index([accountId], map: "accountId")
  @@index([mailId], map: "accountuseremaillog_mailid_index")
  @@index([recipientId], map: "recipientId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountUserFcmLog {
  id         Int      @id @default(autoincrement()) @db.UnsignedInt
  userId     Int      @db.UnsignedInt
  contactId  Int?     @db.UnsignedInt
  sourceId   Int?     @default(0) @db.UnsignedInt
  type       Boolean
  isView     Boolean  @default(false)
  subject    String   @db.VarChar(255)
  message    String   @db.VarChar(255)
  createdAt  DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)
  modifiedAt DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
}

model accountUserFlags {
  id            Int      @id @default(autoincrement())
  accountUserId Int
  flagName      String   @db.VarChar(100)
  flagValue     Boolean  @default(false)
  created       DateTime @default(now()) @db.Timestamp(0)
  modified      DateTime @default(now()) @db.Timestamp(0)

  @@unique([accountUserId, flagName], map: "acFlagUnique")
}

model accountUserGameLevelTrack {
  id                 Int      @id @default(autoincrement())
  accountId          Int
  accountUserId      Int      @unique(map: "accountUserGameLevelTrackId")
  accountGameLevelId Int      @default(0)
  contactsEntered    Int      @default(0)
  contactsActive     Int      @default(0)
  created            DateTime @default(now()) @db.Timestamp(0)
  updated            DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)

  @@index([accountGameLevelId], map: "accountGameLevelId")
}

model accountUserGroup {
  accountUserId  Int
  accountGroupId Int
  created        DateTime @default(now()) @db.Timestamp(0)

  @@unique([accountUserId, accountGroupId], map: "augUserId_augGroupId")
  @@index([accountGroupId], map: "accountGroupId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountUserIncentive {
  id                        Int                               @unique(map: "id") @default(autoincrement())
  accountIncentiveProgramId Int
  userId                    Int
  leadsGenerated            Int?
  awardsEarned              Int?                              @default(0)
  created                   DateTime                          @default(now()) @db.Timestamp(0)
  emailStatus               accountUserIncentive_emailStatus? @default(VALUE_0)
  moneyEarned               Decimal                           @default(0.00) @db.Decimal(9, 2)
  leadsRequired             String?                           @default("0") @db.VarChar(20)
  rank                      String?                           @default("") @db.VarChar(20)
  rankLabel                 String?                           @default("") @db.VarChar(20)
  rankPercentage            String?                           @default("0") @db.VarChar(20)

  @@id([accountIncentiveProgramId, userId])
  @@index([accountIncentiveProgramId], map: "accountIncentiveProgramId")
  @@index([userId], map: "userId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountUserLead {
  id              BigInt?   @unique(map: "id") @db.UnsignedBigInt
  incentiveId     Int
  userId          Int
  leadsGenerated  Int?
  userRank        Int?
  recognitionDate DateTime? @db.DateTime(0)

  @@id([incentiveId, userId])
  @@unique([incentiveId, userId], map: "unique_userLead")
  @@index([incentiveId], map: "incentiveId")
  @@index([userId], map: "userId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountUserLegalshield {
  id                      Int       @id @default(autoincrement()) @db.UnsignedInt
  accountUserId           Int       @unique(map: "accountUserId") @db.UnsignedInt
  associateNumber         Int       @db.UnsignedInt
  associateName           String?   @db.VarChar(255)
  associateEmail          String?   @db.VarChar(255)
  advantageStatus         Boolean   @default(false)
  accessToken             Bytes     @db.Blob
  refreshToken            String?   @db.MediumText
  canCreateEvent          String?   @db.VarChar(100)
  stateOrProvinceOfOrigin String?   @db.VarChar(100)
  tokenExpireTime         Int
  isTokenExpired          Boolean
  connectedDate           DateTime? @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)
  updatedDate             DateTime  @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)

  @@index([accountUserId], map: "lsAcUsr")
  @@index([associateNumber], map: "lsAssNum")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountUserLegalshieldUK {
  id                Int       @id @default(autoincrement()) @db.UnsignedInt
  accountUserId     Int       @unique(map: "accountUserId") @db.UnsignedInt
  associateNumber   String    @db.VarChar(255)
  associateName     String    @db.VarChar(255)
  associateEmail    String    @db.VarChar(255)
  associateUserName String?   @db.VarChar(255)
  advantageStatus   Boolean   @default(false)
  accessToken       Bytes     @db.Blob
  refreshToken      String    @db.Text
  tokenExpireTime   Int
  isTokenExpired    Boolean?
  connectedDate     DateTime? @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)
  updatedDate       DateTime  @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountUserLogs {
  pkId                         Int                        @id @default(autoincrement()) @db.UnsignedInt
  accountId                    Int
  userId                       Int                        @unique(map: "userId")
  userRoleId                   accountUserLogs_userRoleId
  date                         DateTime                   @db.Date
  totalContacts                Int
  totalActiveContacts          Int
  totalContactsSentResDirectly Int                        @default(0)
  totalViewedContacts          Int                        @default(0)
  contactsThisMonth            Int
  activeContactsThisMonth      Int
  totalEmailsSent              Int
  totalEmailsSentThisMonth     Int
  monthlyContacts              String?                    @db.VarChar(5000)
  monthlyEmailsLast11Months    String                     @db.VarChar(1000)
  mailLogStatsForGroupLeader   String?                    @db.VarChar(5000)
  monthlyEmails                String                     @db.VarChar(1000)
  monthlyClickedLinks          String                     @db.VarChar(1000)
  contacts30Days               String                     @db.VarChar(3000)
  totalUsers                   Int
  totalPaidUsers               Int
  totalTrialUsers              Int?                       @default(0)
  users45Days                  String                     @db.VarChar(5000)
  contactsAssignedToCampaign   Int
  totalOptedInContacts         Int
  resourceStats                String?                    @db.Text
  eventStats                   String?                    @db.Text
  totalExposedContacts         Int                        @default(0)

  @@index([accountId], map: "accountId")
  @@index([accountId], map: "accountId_2")
  @@index([mailLogStatsForGroupLeader(length: 255)], map: "mailLogStatsForGroupLeader")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountUserMobileToken {
  id          Int       @id @default(autoincrement()) @db.UnsignedInt
  userId      Int       @db.UnsignedInt
  deviceToken String    @db.VarChar(255)
  deviceOs    Boolean
  osVersion   String?   @db.VarChar(25)
  deviceName  String?   @db.VarChar(50)
  buildName   String?   @db.VarChar(25)
  buildNumber String?   @db.VarChar(25)
  createdAt   DateTime  @default(now()) @db.Timestamp(0)
  modifiedAt  DateTime? @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)

  @@index([modifiedAt], map: "accountUserMobileToken_modifiedAt_index")
  @@index([userId], map: "accountUserMobileToken_userId_index")
  @@index([userId, deviceToken], map: "accountUserMobileToken_userId_mobileToken_index")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountUserWarnedSuspend {
  pkId                       Int       @id @default(autoincrement()) @db.UnsignedInt
  userId                     Int       @unique(map: "userId") @db.UnsignedInt
  spamComplaintStatus        Boolean   @default(false)
  noOfSpamComplaints         Int       @default(0) @db.SmallInt
  deactivationSpamComplaints Int       @default(0) @db.SmallInt
  spamWarnDate               DateTime? @db.Date
  spamDeactivateDate         DateTime? @db.Date
  lowOptinStatus             Boolean   @default(false)
  lowOptinPreWarnDate        DateTime? @db.Date
  lowOptinWarnDate           DateTime? @db.Date
  lowOptinDeactivateDate     DateTime? @db.Date
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accountUserZoom {
  id                 Int       @id @default(autoincrement()) @db.UnsignedInt
  accountUserId      Int       @unique(map: "accountUserId") @db.UnsignedInt
  associateNumber    String    @db.VarChar(255)
  associateAccountId String    @db.VarChar(255)
  associateName      String?   @db.VarChar(255)
  associateEmail     String?   @db.VarChar(255)
  accessToken        Bytes     @db.Blob
  refreshToken       String    @db.Text
  tokenExpireTime    Int
  isTokenExpired     Boolean
  connectedDate      DateTime? @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)
  updatedDate        DateTime  @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)

  @@index([accountUserId], map: "zoomAcUsr")
  @@index([associateNumber], map: "zoomAssNum")
}

model achievementGroup {
  pkId          Int @id @default(autoincrement()) @db.UnsignedInt
  achievementId Int
  groupId       Int

  @@unique([achievementId, groupId], map: "AchievementGroupId")
  @@index([achievementId], map: "achievementId")
  @@index([groupId], map: "groupId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model achievements {
  id               Int      @id @default(autoincrement())
  accountId        Int
  type             Boolean
  activity         Int      @db.TinyInt
  goal             Int
  period           Boolean
  description      String?  @db.Text
  image            String?  @db.VarChar(255)
  allGroupSelected Boolean  @default(true)
  status           Boolean  @default(true)
  created          DateTime @default(now()) @db.Timestamp(0)
  createdBy        Int
  modified         DateTime @default(now()) @db.Timestamp(0)
  modifiedBy       Int?

  @@index([accountId], map: "accountId")
  @@index([activity], map: "activity")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model adminDashboardStatistics {
  pkId                  Int @id @default(autoincrement()) @db.UnsignedInt
  userId                Int @unique(map: "userId") @db.UnsignedInt
  totalAccounts         Int @db.UnsignedInt
  totalContacts         Int @db.UnsignedInt
  totalUsers            Int @db.UnsignedInt
  companyPaidUsers      Int @db.UnsignedInt
  totalPaidPassThrUsers Int @db.UnsignedInt
  last30DaysUsers       Int @db.UnsignedInt
}

model attendance {
  calendar    String? @db.VarChar(20)
  calendar_id Int?    @db.UnsignedInt
  entry_id    Int     @id @db.UnsignedInt
  unix_date   Int?    @db.UnsignedInt
  associates  Int?    @db.UnsignedSmallInt
  guests      Int?    @db.UnsignedSmallInt
}

model billingReportChargify {
  id             Int      @id @default(autoincrement()) @db.UnsignedInt
  site           String   @db.VarChar(255)
  batchGroupId   Int      @db.UnsignedInt
  batchId        Int      @db.UnsignedInt
  transactionId  String?  @db.VarChar(255)
  type           String?  @db.VarChar(255)
  created        DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)
  description    String?  @db.VarChar(255)
  fee            Float?
  amount         Float?
  amountRefunded Float?
  totalPaid      Float?
  status         String?  @db.VarChar(255)
  customerName   String?  @db.VarChar(255)
  customerEmail  String?  @db.VarChar(255)
  accountId      Int?     @db.UnsignedInt
  userId         Int?     @db.UnsignedInt
  groupId        Int?     @db.UnsignedInt
  accountName    String?  @db.VarChar(255)
  groupName      String?  @db.VarChar(255)

  @@index([batchGroupId], map: "batchGroupId")
  @@index([batchId], map: "batchId")
}

model bizCardStats {
  id              Int      @id @default(autoincrement())
  contactId       Int
  bizCardButtonId Int
  created         DateTime @default(now()) @db.Timestamp(0)

  @@index([bizCardButtonId], map: "bizCardButtonId")
  @@index([contactId], map: "contactId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model broadcastEmailForAccounts {
  id                      Int      @id @default(autoincrement())
  accountBroadcastEmailId Int
  accountId               Int
  processToTempTable      Boolean? @default(false)
  totalRecipients         Int?     @default(0)
  domainName              String?  @db.VarChar(150)
  isCustomDomain          Int?     @default(0)
  created                 DateTime @default(now()) @db.Timestamp(0)

  @@unique([accountBroadcastEmailId, accountId], map: "broadcastEmailForAccountIds")
  @@index([accountBroadcastEmailId], map: "accountBroadcastEmailId")
}

model calendars {
  t                  DateTime                @db.DateTime(0)
  id                 Int                     @id @default(autoincrement()) @db.UnsignedInt
  account_id         Int?                    @db.UnsignedInt
  calendar           String?                 @db.VarChar(20)
  alias              String?                 @db.VarChar(20)
  password           String?                 @db.VarChar(20)
  description        String?                 @db.TinyText
  moderator_name     String?                 @db.TinyText
  moderator_email    String?                 @db.TinyText
  moderator_password String                  @default("legalshieldrocks") @db.VarChar(20)
  show_popdown       calendars_show_popdown?
  display_ads        calendars_display_ads?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model campaignMailLogEmailStatsRelMongoDB {
  pkId            Int @id @default(autoincrement()) @db.UnsignedInt
  accountId       Int @db.UnsignedInt
  campaignId      Int @db.UnsignedInt
  campaignEmailId Int @db.UnsignedInt
  sentMailCount   Int @db.UnsignedInt
  openCount       Int @default(0) @db.UnsignedInt
  clickCount      Int @default(0) @db.UnsignedInt
  optOutCount     Int @default(0) @db.UnsignedInt
  complaintCount  Int @default(0) @db.UnsignedInt

  @@unique([accountId, campaignId, campaignEmailId], map: "accountCampaignEmailId")
  @@index([accountId], map: "accountId")
  @@index([campaignEmailId], map: "campaignEmailId")
  @@index([campaignId], map: "campaignId")
}

model checkoutPageSubscription {
  pkId           Int      @id @default(autoincrement()) @db.UnsignedInt
  email          String?  @default("") @db.VarChar(255)
  subscriptionId Int?     @unique(map: "cps_subscriptionId") @db.UnsignedInt
  customerId     Int?     @db.UnsignedInt
  created        DateTime @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model configs {
  id       Int          @id @default(autoincrement()) @db.UnsignedInt
  name     String       @unique(map: "name") @db.VarChar(255)
  type     configs_type
  value    String?      @db.VarChar(255)
  note     String?      @db.VarChar(255)
  created  DateTime     @default(now()) @db.Timestamp(0)
  modified DateTime?    @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model contactCallTextActivity {
  id           BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  contactId    Int      @db.UnsignedInt
  activityType Int      @db.TinyInt
  createdOn    DateTime @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model contactEvents {
  id        Int       @id @default(autoincrement()) @db.UnsignedInt
  contactId Int?
  eventId   Int?
  emailFlag Boolean?  @default(false)
  pushFlag  Boolean?  @default(false)
  status    Boolean?  @default(false)
  created   DateTime? @default(now()) @db.Timestamp(0)
  eventDate DateTime? @db.Date
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model contactExportBatches {
  id          Int      @id @default(autoincrement()) @db.UnsignedInt
  userId      Int      @db.UnsignedInt
  recordsFrom Int      @db.UnsignedInt
  recordsTo   Int      @db.UnsignedInt
  isSentToSQS Boolean  @default(false)
  status      Boolean  @default(false)
  fileName    String?  @db.VarChar(100)
  created     DateTime @default(now()) @db.Timestamp(0)
  modified    DateTime @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model contactJourneyActivity {
  accountId        Int
  contactId        Int
  contactJourneyId Int
  created          DateTime @default(now()) @db.Timestamp(0)
  createdBy        Int?
  modified         DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  modifiedBy       Int?

  @@id([accountId, contactId, contactJourneyId])
}

model contactResourceIncentives {
  id               Int       @id @default(autoincrement())
  accountId        Int?
  accountContactId Int?
  userId           Int?
  incentiveId      Int?
  resourceId       Int?
  completionDate   DateTime? @db.DateTime(0)
  created          DateTime? @db.DateTime(0)
  modified         DateTime? @db.DateTime(0)

  @@unique([accountContactId, incentiveId, resourceId], map: "unique_contactResIncentive")
  @@index([accountContactId], map: "accountContactId")
  @@index([incentiveId], map: "incentiveId")
  @@index([resourceId], map: "resourceId")
  @@index([userId], map: "userId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model contactSignificantDates {
  id              Int       @id @default(autoincrement()) @db.UnsignedInt
  accountId       Int?      @db.UnsignedInt
  contactId       Int
  significantDate DateTime? @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)
  isRecurring     Boolean?  @default(false)
  typeId          Int
  remindMe        Int       @default(0) @db.TinyInt
  createdBy       Int?      @db.UnsignedInt
  created         DateTime? @default(now()) @db.Timestamp(0)
  modifiedBy      Int?      @db.UnsignedInt
  modified        DateTime? @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)

  @@unique([id, contactId], map: "unique_idContactId")
  @@index([accountId], map: "accountId")
  @@index([contactId], map: "contactId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model contactTags {
  id          Int      @id @default(autoincrement()) @db.UnsignedInt
  accountId   Int
  name        String   @db.VarChar(256)
  description String?  @db.MediumText
  created     DateTime @default(now()) @db.Timestamp(0)
  createdBy   Int
  modified    DateTime @default(now()) @db.Timestamp(0)
  modifiedBy  Int?
  tagType     Int      @default(0)
  internalId  BigInt?  @default(0) @db.UnsignedBigInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model contactsBizCardActivityLog {
  id         Int                                 @id @default(autoincrement()) @db.UnsignedInt
  userId     Int                                 @db.UnsignedInt
  contactId  Int                                 @db.UnsignedInt
  accountId  Int                                 @default(0)
  status     contactsBizCardActivityLog_status?
  sentFrom   contactsBizCardActivityLog_sentFrom @default(web)
  sentMethod Boolean                             @default(false)
  created    DateTime                            @default(now()) @db.Timestamp(0)

  @@index([contactId], map: "contactId")
  @@index([accountId], map: "contactsbizcardactivitylog_accountid_index")
  @@index([created], map: "contactsbizcardactivitylog_created_index")
  @@index([sentFrom], map: "contactsbizcardactivitylog_sentfrom_index")
  @@index([status], map: "contactsbizcardactivitylog_status_index")
  @@index([userId], map: "userId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model contactsEventActivityLog {
  id         Int                               @id @default(autoincrement()) @db.UnsignedInt
  contactId  Int                               @db.UnsignedInt
  accountId  Int                               @db.UnsignedInt
  eventId    Int                               @db.UnsignedInt
  eventName  String                            @default("") @db.VarChar(255)
  userId     Int                               @db.UnsignedInt
  status     contactsEventActivityLog_status
  sentFrom   contactsEventActivityLog_sentFrom @default(web)
  sentMethod Boolean                           @default(false)
  created    DateTime                          @default(now()) @db.Timestamp(0)
  eventDate  DateTime?                         @db.Date

  @@index([contactId], map: "contactId")
  @@index([eventId], map: "eventId")
  @@index([eventName], map: "userId")
  @@fulltext([eventName], map: "eventName")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model contactsResourceActivityLog {
  id                 Int                                  @id @default(autoincrement()) @db.UnsignedInt
  contactId          Int                                  @db.UnsignedInt
  accountId          Int                                  @default(0)
  resourceId         Int                                  @db.UnsignedInt
  resourceType       String                               @default("") @db.VarChar(30)
  resourceName       String                               @default("") @db.VarChar(255)
  userId             Int                                  @db.UnsignedInt
  status             contactsResourceActivityLog_status?
  sentFrom           contactsResourceActivityLog_sentFrom @default(web)
  sentMethod         Boolean                              @default(false)
  created            DateTime                             @default(now()) @db.Timestamp(0)
  contactHash        String?                              @db.Char(128)
  isContactCaptured  Boolean?                             @default(false)
  isPersonalResource Int?                                 @default(0) @db.TinyInt
  fingerprint        String?                              @db.VarChar(255)
  isGenuine          Boolean?                             @default(true)

  @@index([contactHash], map: "con_hash")
  @@index([contactId], map: "contactId")
  @@index([accountId], map: "contactsResourceActivityLog_accountId_index")
  @@index([created], map: "contactsResourceActivityLog_created_index")
  @@index([sentFrom], map: "contactsResourceActivityLog_sentFrom_index")
  @@index([status], map: "contactsResourceActivityLog_status_index")
  @@index([isPersonalResource], map: "isPersonalResource_index")
  @@index([resourceId], map: "resourceId")
  @@index([userId], map: "userId")
  @@fulltext([resourceName], map: "resourceName")
}

model contactsVisitLog {
  pkId        Int       @id @default(autoincrement()) @db.UnsignedInt
  userId      Int       @db.UnsignedInt
  fingerprint String    @db.VarChar(255)
  created     DateTime  @default(now()) @db.Timestamp(0)
  lastUsed    DateTime? @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)

  @@index([userId], map: "contactsVisitLog_userId_index")
}

model countries {
  id       Int    @id @default(autoincrement())
  sortname String @db.VarChar(3)
  name     String @db.VarChar(150)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model countryPhoneCode {
  id          Int    @id @default(autoincrement())
  phoneCode   String @db.VarChar(10)
  countryCode Int
  countryName String @db.VarChar(100)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model courseModules {
  id                Int                   @id @default(autoincrement()) @db.UnsignedInt
  accountId         Int?                  @db.UnsignedInt
  courseId          Int?                  @db.UnsignedInt
  moduleName        String?               @db.VarChar(255)
  moduleDescription String?               @db.MediumText
  moduleThumbnail   String?               @db.VarChar(255)
  moduleVideo       String?               @db.VarChar(255)
  mediaName         String?               @db.VarChar(200)
  videoDuration     String?               @db.VarChar(255)
  moduleDuration    String?               @db.VarChar(255)
  moduleOrder       Int?
  status            courseModules_status? @default(VALUE_1)
  created           DateTime?             @default(now()) @db.Timestamp(0)
  createdBy         Int?                  @db.UnsignedInt
  modified          DateTime?             @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  modifiedBy        Int?                  @db.UnsignedInt

  @@index([accountId], map: "accountId")
  @@index([courseId], map: "cm_course_id")
  @@index([createdBy], map: "createdBy")
  @@index([modifiedBy], map: "modifiedBy")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model directory_country_regions {
  id           Int     @id @default(autoincrement()) @db.UnsignedMediumInt
  country_id   String  @default("0") @db.VarChar(4)
  code         String  @default("") @db.VarChar(32)
  default_name String? @db.VarChar(255)

  @@index([country_id], map: "FK_REGION_COUNTRY")
}

model engagementResourceIncentives {
  id               Int       @id @default(autoincrement())
  accountId        Int?
  accountContactId Int?
  userId           Int?
  incentiveId      Int?
  resourceId       Int?
  completionDate   DateTime? @db.DateTime(0)
  created          DateTime? @db.DateTime(0)
  modified         DateTime? @db.DateTime(0)

  @@unique([accountContactId, incentiveId, resourceId], map: "unique_userIncentive")
  @@index([accountContactId], map: "accountContactId")
  @@index([incentiveId], map: "incentiveId")
  @@index([resourceId], map: "resourceId")
  @@index([userId], map: "userId")
}

model entities {
  id        Int      @id @default(autoincrement()) @db.UnsignedInt
  name      String   @unique(map: "name") @default("") @db.VarChar(100)
  createdAt DateTime @default(now()) @db.Timestamp(0)
}

model entries1 {
  t               DateTime       @default(now()) @db.Timestamp(0)
  id              Int            @id @default(autoincrement()) @db.UnsignedInt
  pin             Int?           @db.UnsignedSmallInt
  calendar_id     Int?           @db.UnsignedInt
  event_prefix    String?        @db.TinyText
  event_type      String?        @db.TinyText
  month           Int?           @db.UnsignedTinyInt
  day             Int?           @db.UnsignedTinyInt
  year            Int?           @db.UnsignedSmallInt
  reg_time        Int?           @db.UnsignedSmallInt
  start_time      Int?           @db.UnsignedSmallInt
  end_time        Int?           @db.UnsignedSmallInt
  timezone        String?        @db.TinyText
  repeat_day      Int?           @db.UnsignedTinyInt
  repeat_type     String?        @db.Char(1)
  repeat_num      Int?           @db.TinyInt
  endmonth        Int?           @db.UnsignedTinyInt
  endday          Int?           @db.UnsignedTinyInt
  endyear         Int?           @db.UnsignedSmallInt
  hidden          String?        @db.Text
  idSeries        Int?           @db.UnsignedInt
  dateSeries      String?        @db.TinyText
  exceptions      String?        @db.Text
  speaker         String?        @db.TinyText
  url_image       String?        @db.Text
  photofmt        Int?           @db.TinyInt
  url_description String?        @db.Text
  description     String?        @db.Text
  contact1        String?        @db.TinyText
  contact2        String?        @db.TinyText
  directions      String?        @db.Text
  url_map         String?        @db.Text
  rsvp            entries1_rsvp?
  rsvp_cap        Int?           @db.UnsignedSmallInt
  rsvp_emails     String?        @db.Text

  @@index([calendar_id], map: "calendar_id")
}

model entries_bkp {
  t               DateTime          @default(now()) @db.Timestamp(0)
  id              Int               @id @default(autoincrement()) @db.UnsignedInt
  pin             Int?              @db.UnsignedSmallInt
  calendar_id     Int?              @db.UnsignedInt
  event_prefix    String?           @db.TinyText
  event_type      String?           @db.TinyText
  month           Int?              @db.UnsignedTinyInt
  day             Int?              @db.UnsignedTinyInt
  year            Int?              @db.UnsignedSmallInt
  reg_time        Int?              @db.UnsignedSmallInt
  start_time      Int?              @db.UnsignedSmallInt
  end_time        Int?              @db.UnsignedSmallInt
  timezone        String?           @db.TinyText
  repeat_day      Int?              @db.UnsignedTinyInt
  repeat_type     String?           @db.Char(1)
  repeat_num      Int?              @db.TinyInt
  endmonth        Int?              @db.UnsignedTinyInt
  endday          Int?              @db.UnsignedTinyInt
  endyear         Int?              @db.UnsignedSmallInt
  hidden          String?           @db.Text
  idSeries        Int?              @db.UnsignedInt
  dateSeries      String?           @db.TinyText
  exceptions      String?           @db.Text
  speaker         String?           @db.TinyText
  url_image       String?           @db.Text
  photofmt        Int?              @db.TinyInt
  url_description String?           @db.Text
  description     String?           @db.Text
  contact1        String?           @db.TinyText
  contact2        String?           @db.TinyText
  directions      String?           @db.Text
  url_map         String?           @db.Text
  rsvp            entries_bkp_rsvp?
  rsvp_cap        Int?              @db.UnsignedSmallInt
  rsvp_emails     String?           @db.Text

  @@index([calendar_id], map: "calendar_id")
  @@index([id, calendar_id], map: "id")
  @@index([id, calendar_id], map: "id_2")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model eventCategory {
  id                 Int       @id @default(autoincrement())
  accountId          Int
  name               String?   @db.VarChar(255)
  visibility         Boolean   @default(false)
  isPersonalCategory Boolean   @default(false)
  created            DateTime  @default(now()) @db.Timestamp(0)
  createdBy          Int?
  modified           DateTime? @default(now()) @db.Timestamp(0)
  modifiedBy         Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model eventFollowUpDetails {
  id                Int                           @id @default(autoincrement()) @db.UnsignedInt
  eventId           Int                           @db.UnsignedInt
  contactId         Int                           @db.UnsignedInt
  eventDate         DateTime                      @db.Date
  eventFollowUpTime Int                           @db.UnsignedInt
  timeUnit          eventFollowUpDetails_timeUnit

  @@unique([eventId, eventDate, contactId], map: "eventFollowUpUnique")
}

model eventGroup {
  eventGroupId Int  @id @default(autoincrement())
  eventId      Int?
  groupId      Int?
  status       Int?

  @@index([eventId], map: "eventId")
  @@index([eventId, eventGroupId], map: "eventId_2")
}

model eventMessage {
  eventMessageId Int       @id @default(autoincrement())
  userId         Int?
  messageType    Int?
  accountId      Int?
  startDate      DateTime? @db.DateTime(0)
  description    String?   @db.MediumText
  speakerName    String?   @db.VarChar(256)
  speakerEmail   String?   @db.VarChar(256)
  speakerPhone   String?   @db.VarChar(256)
  speakerProfile String?   @db.MediumText
  location       String?   @db.VarChar(100)
  address        String?   @db.VarChar(256)
  createdBy      Int?
  createdOn      DateTime? @db.DateTime(0)
  modifiedBy     Int?
  modifiedOn     DateTime? @db.DateTime(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model eventStates {
  id         Int    @id @default(autoincrement()) @db.UnsignedInt
  eventId    Int    @db.UnsignedInt
  eventState String @db.VarChar(10)

  @@index([eventId], map: "eventId")
  @@index([eventState], map: "eventState")
}

model event_sessions {
  ssid         String   @id @db.Char(20)
  date_created DateTime @db.DateTime(0)
  account_id   Int?     @db.UnsignedInt
  account_name String?  @db.VarChar(20)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model events {
  id                    Int                  @id @default(autoincrement())
  LsId                  Int?
  eventParentId         Int?
  eventCategoryId       Int?
  eventCategoryType     Int?                 @default(0)
  userId                Int
  accountId             Int
  address               String?              @db.VarChar(256)
  locationLatitude      String?              @db.VarChar(100)
  locationLongitude     String?              @db.VarChar(100)
  mapLink               String?              @db.VarChar(250)
  eventTitle            String?              @db.VarChar(256)
  eventName             String?              @db.VarChar(256)
  eventType             Int?
  eventTimeZone         Int?
  eventStartTime        DateTime?            @db.DateTime(0)
  eventEndTime          DateTime?            @db.DateTime(0)
  recurringEndDate      DateTime?            @db.DateTime(0)
  eventRepeatDay        Boolean?
  eventDialUpNumber     String?              @db.VarChar(100)
  eventDialUpPin        String?              @db.VarChar(100)
  eventConfLink         String?              @db.VarChar(255)
  eventState            String?              @db.VarChar(100)
  eventCity             String?              @db.VarChar(100)
  eventDescription      String?              @db.LongText
  eventExtDescription   String?              @db.MediumText
  longMessage           String?              @db.LongText
  shortMessage          String?              @db.LongText
  eventSubject          String?              @db.LongText
  eventSpeakerName      String?              @db.VarChar(256)
  eventSpeakerEmail     String?              @db.VarChar(100)
  eventSpeakerPhone     String?              @db.VarChar(100)
  eventSpeakerProfile   String?              @db.LongText
  recurringEvent        Int?
  convertedStartTime    DateTime?            @db.DateTime(0)
  convertedEndTime      DateTime?            @db.DateTime(0)
  convertededRecEndDate DateTime?            @db.DateTime(0)
  eventImage            String?              @db.VarChar(250)
  status                Int?
  createdBy             Int?
  created_at            DateTime?            @db.DateTime(0)
  modifiedBy            Int?
  updated_at            DateTime?            @db.DateTime(0)
  postalCode            String?              @db.VarChar(50)
  period                Int?
  notifyOption          events_notifyOption?
  notifyTime            DateTime?            @db.DateTime(0)
  rawNotifyTime         DateTime?            @db.DateTime(0)
  LSEventType           String?              @db.VarChar(200)
  eventRegisterTime     DateTime?            @db.DateTime(0)
  imageUrl              String?              @db.VarChar(255)
  photoFormat           String?              @db.VarChar(100)
  urlDescription        String?              @db.LongText
  contact1              String?              @db.LongText
  contact2              String?              @db.LongText
  contactPhone          String?              @db.VarChar(200)
  importCalendar        Boolean?
  deletedRecord         String?              @db.LongText
  editedecord           String?              @db.LongText
  contactEmail          String?              @db.VarChar(200)
  eventCountry          String?              @db.VarChar(200)
  isRsvpGoal            Boolean?
  rsvpGoal              Int?
  eventGroupStatus      Boolean?
  isEdited              Int?                 @default(0) @db.TinyInt
  iseventRegisterTime   Boolean?
  isPublicVisible       Boolean?             @default(true)
  isPersonalEvent       Boolean?
  userFollowUp          String?              @db.VarChar(2000)
  disableSharing        Boolean?             @default(false)

  @@index([LsId], map: "LsId")
  @@index([accountId], map: "e_accountId")
  @@index([eventCategoryId], map: "e_eventCategoryId")
  @@index([userId], map: "e_userId")
  @@index([eventStartTime], map: "eventStartTime")
  @@index([notifyTime], map: "notifyTime")
  @@index([notifyTime, eventState, eventParentId], map: "notifyTime_2")
  @@fulltext([eventDescription, eventName], map: "eventDescription")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model eventsNew {
  id                    Int                     @id @default(autoincrement())
  LsId                  Int?
  eventParentId         Int?
  eventCategoryId       Int?
  userId                Int
  accountId             Int
  address               String?                 @db.VarChar(256)
  locationLatitude      String?                 @db.VarChar(100)
  locationLongitude     String?                 @db.VarChar(100)
  mapLink               String?                 @db.VarChar(250)
  eventTitle            String?                 @db.VarChar(256)
  eventName             String?                 @db.VarChar(256)
  eventType             Int?
  eventTimeZone         Int?                    @default(97)
  eventStartTime        DateTime?               @db.DateTime(0)
  eventEndTime          DateTime?               @db.DateTime(0)
  recurringEndDate      DateTime?               @db.DateTime(0)
  eventRepeatDay        Boolean?
  eventDialUpNumber     String?                 @db.VarChar(100)
  eventDialUpPin        String?                 @db.VarChar(100)
  eventConfLink         String?                 @db.VarChar(255)
  eventState            String?                 @db.VarChar(100)
  eventCity             String?                 @db.VarChar(100)
  eventDescription      String?                 @db.Text
  eventExtDescription   String?                 @db.Text
  longMessage           String?                 @db.Text
  shortMessage          String?                 @db.Text
  eventSubject          String?                 @db.Text
  eventSpeakerName      String?                 @db.VarChar(256)
  eventSpeakerEmail     String?                 @db.VarChar(100)
  eventSpeakerPhone     String?                 @db.VarChar(100)
  eventSpeakerProfile   String?                 @db.Text
  recurringEvent        Int?
  convertedStartTime    DateTime?               @db.DateTime(0)
  convertedEndTime      DateTime?               @db.DateTime(0)
  convertededRecEndDate DateTime?               @db.DateTime(0)
  eventImage            String?                 @db.VarChar(250)
  status                Int?
  createdBy             Int?
  created_at            DateTime?               @db.DateTime(0)
  modifiedBy            Int?
  updated_at            DateTime?               @db.DateTime(0)
  postalCode            String?                 @db.VarChar(50)
  period                Int?
  notifyOption          eventsNew_notifyOption?
  notifyTime            DateTime?               @db.DateTime(0)
  LSEventType           String?                 @db.VarChar(200)
  eventRegisterTime     DateTime?               @db.DateTime(0)
  imageUrl              String?                 @db.VarChar(255)
  photoFormat           String?                 @db.VarChar(100)
  urlDescription        String?                 @db.Text
  contact1              String?                 @db.Text
  contact2              String?                 @db.Text
  contactPhone          String?                 @db.VarChar(200)
  importCalendar        Boolean?
  deletedRecord         String?                 @db.Text
  editedecord           String?                 @db.Text
  contactEmail          String?                 @db.VarChar(200)
  eventCountry          String?                 @db.VarChar(200)
  isRsvpGoal            Boolean?
  rsvpGoal              Int?

  @@index([eventType], map: "eventType")
  @@index([notifyTime, eventState, eventParentId], map: "notifyTime")
}

model exportUserRequests {
  id         Int       @id @default(autoincrement())
  accountId  Int
  getParams  String?   @db.Text
  fromDate   DateTime? @db.DateTime(0)
  toDate     DateTime? @db.DateTime(0)
  sentStatus Boolean?
  created    DateTime  @default(now()) @db.Timestamp(0)
  createdBy  Int
}

model exportUserStats {
  id                       Int  @id @default(autoincrement())
  exportUserRequestsId     Int
  accountId                Int
  userId                   Int
  totalExposed             Int? @default(0)
  noOfCallRequestsSent     Int? @default(0)
  noOfEventInvitationsSent Int? @default(0)
}

model ftStopwords {
  pkId  Int     @id @default(autoincrement()) @db.UnsignedInt
  value String? @db.VarChar(30)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model iCueAccount {
  id               Int     @id @default(autoincrement()) @db.UnsignedInt
  accountId        Int     @db.UnsignedInt
  adminUserName    String  @db.VarChar(100)
  password         String  @db.VarChar(100)
  offerCode        String  @db.VarChar(100)
  availability     Int     @db.UnsignedTinyInt
  subDomain        String  @db.VarChar(50)
  premiumOfferCode String? @db.VarChar(255)
}

model migrations {
  id        Int    @id @default(autoincrement()) @db.UnsignedInt
  migration String @db.VarChar(255)
  batch     Int
}

model mobileBuild {
  id            Int      @id @default(autoincrement()) @db.SmallInt
  iOSPBLS       String?  @db.VarChar(50)
  iOSRF         String?  @db.VarChar(50)
  iOSPBLSUK     String?  @db.VarChar(50)
  androidPBLS   String?  @db.VarChar(50)
  androidRF     String?  @db.VarChar(50)
  androidPBLSUK String?  @db.VarChar(50)
  created       DateTime @default(now()) @db.Timestamp(0)
  createdBy     Int
  modified      DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)
  modifiedBy    Int
}

model mobileBuildVersions {
  id        Int      @id @default(autoincrement()) @db.UnsignedInt
  name      String   @db.VarChar(100)
  shortName String   @db.VarChar(100)
  version   String   @db.VarChar(100)
  updatedAt DateTime @default(now()) @db.Timestamp(0)

  @@unique([name, shortName], map: "mobilebuildversions_name_shortname_unique")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model mongoMigration {
  id                    Int       @id @default(autoincrement()) @db.UnsignedInt
  dataProcessedTillDate DateTime? @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)
  isCronRunning         Boolean   @default(false)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model netreadyIntegration {
  id        Int    @id @default(autoincrement()) @db.UnsignedInt
  userId    Int    @db.UnsignedInt
  accountId Int    @db.UnsignedInt
  guid      String @db.VarChar(255)

  @@unique([userId, accountId], map: "netreadyintegration_userid_accountid_unique")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model notification {
  id          Int      @id @default(autoincrement())
  sentBy      Int
  created     DateTime @default(now()) @db.DateTime(0)
  message     String?  @db.MediumText
  sendToRoles String   @db.VarChar(255)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model owners {
  t           DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)
  calendar    String?  @db.VarChar(20)
  alias       String?  @db.VarChar(20)
  password    String?  @db.VarChar(20)
  description String?  @db.TinyText
  owner_name  String?  @db.TinyText
  owner_email String?  @db.TinyText

  @@ignore
}

model paypal_transactions {
  account_name   String   @id @db.VarChar(20)
  date_created   DateTime @db.DateTime(0)
  subscr_id      String?  @db.Text
  subscr_date    String?  @db.Text
  txn_type       String?  @db.Text
  txn_id         String?  @db.Text
  payment_status String?  @db.Text
  payment_gross  String?  @db.Text
  payment_date   String?  @db.Text
  paid_through   String?  @db.Char(8)
}

model permissions {
  id        Int      @id @default(autoincrement()) @db.UnsignedInt
  name      String   @unique(map: "name") @default("") @db.VarChar(100)
  createdAt DateTime @default(now()) @db.Timestamp(0)
}

model personal_access_tokens {
  id             BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  tokenable_type String    @db.VarChar(255)
  tokenable_id   BigInt    @db.UnsignedBigInt
  name           String    @db.VarChar(255)
  token          String    @unique(map: "personal_access_tokens_token_unique") @db.VarChar(64)
  abilities      String?   @db.Text
  last_used_at   DateTime? @db.Timestamp(0)
  expires_at     DateTime? @db.Timestamp(0)
  created_at     DateTime? @db.Timestamp(0)
  updated_at     DateTime? @db.Timestamp(0)

  @@index([tokenable_type, tokenable_id], map: "personal_access_tokens_tokenable_type_tokenable_id_index")
}

model photos {
  id          Int      @id @default(autoincrement()) @db.UnsignedInt
  calendar_id Int?     @db.UnsignedInt
  name        String?  @db.Text
  type        String?  @db.VarChar(10)
  image       Bytes    @db.Blob
  last_used   DateTime @db.Date
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model preReleased {
  id Int

  @@ignore
}

model privilege {
  roleId     Int              @default(0)
  resourceId Int              @default(0)
  access     String?          @db.MediumText
  assert     privilege_assert @default(No)

  @@id([roleId, resourceId])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model prospectRedemptionCode {
  id             Int                           @id @default(autoincrement())
  code           String                        @db.VarChar(20)
  redemptionLink String                        @db.VarChar(255)
  isUsed         prospectRedemptionCode_isUsed @default(VALUE_0)
  accountUserId  Int                           @default(0)
  name           String?                       @db.VarChar(255)
  email          String?                       @db.VarChar(255)
  phone          String?                       @db.VarChar(15)
  updatedAt      DateTime?                     @db.Timestamp(0)

  @@index([email(length: 191)], map: "prospectRedemptionCodeEmail")
  @@index([phone], map: "prospectRedemptionCodePhone")
}

model radiusData {
  countryCode    String?  @db.VarChar(2)
  zipCode        String?  @db.VarChar(5)
  city           String?  @db.VarChar(64)
  aliases        String?  @db.VarChar(255)
  county         String?  @db.VarChar(64)
  countryFips    String?  @db.VarChar(3)
  state          String?  @db.VarChar(48)
  stateCode      String?  @db.VarChar(2)
  stateFips      String?  @db.VarChar(2)
  timezone       String?  @db.VarChar(10)
  daylightSaving String?  @db.VarChar(1)
  latitude       Decimal? @db.Decimal(13, 9)
  longitude      Decimal? @db.Decimal(13, 9)
  id             Int      @id @default(autoincrement())

  @@index([countryCode, zipCode, city, state], map: "countryCode")
  @@index([city], map: "e_city")
  @@index([countryCode], map: "e_countryCode")
  @@index([latitude], map: "e_latitude")
  @@index([longitude], map: "e_longitude")
  @@index([state], map: "e_state")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model radiusData1 {
  countryCode    String?  @db.VarChar(2)
  zipCode        String?  @db.VarChar(5)
  city           String?  @db.VarChar(64)
  aliases        String?  @db.VarChar(255)
  county         String?  @db.VarChar(64)
  countryFips    String?  @db.VarChar(3)
  state          String?  @db.VarChar(48)
  stateCode      String?  @db.VarChar(2)
  stateFips      String?  @db.VarChar(2)
  timezone       String?  @db.VarChar(10)
  daylightSaving String?  @db.VarChar(1)
  latitude       Decimal? @db.Decimal(13, 9)
  longitude      Decimal? @db.Decimal(13, 9)
  id             Int      @default(0)

  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model resource {
  id       Int      @id @default(autoincrement())
  name     String   @unique(map: "resource_name_uindex") @db.VarChar(100)
  parent   String?  @db.VarChar(150)
  created  DateTime @default(now()) @db.Timestamp(0)
  modified DateTime @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model role {
  id                     Int      @id @default(autoincrement())
  name                   String   @default("") @db.VarChar(30)
  InheritPermissionsFrom String?  @db.VarChar(150)
  rank                   Int?
  createdAt              DateTime @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model roleEntityPermission {
  id           Int @id @default(autoincrement()) @db.UnsignedInt
  roleId       Int
  entityId     Int
  permissionId Int

  @@index([entityId], map: "rePermission_entityId")
  @@index([permissionId], map: "rePermission_permissionId")
  @@index([roleId], map: "rePermission_roleId")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rsvps {
  entry_id   Int?      @db.UnsignedInt
  event_date DateTime? @db.Date
  attendees  String?   @db.MediumText

  @@ignore
}

model searches {
  id          Int     @id @default(autoincrement()) @db.UnsignedInt
  account_id  Int?    @db.UnsignedInt
  search_name String? @db.Text
  terms       String? @db.MediumText
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model serviceTitanIntegration {
  id        Int @id @default(autoincrement()) @db.UnsignedInt
  userId    Int @db.UnsignedInt
  accountId Int @db.UnsignedInt
  tenantId  Int @unique(map: "st_unique_tenantId")

  @@unique([userId, accountId], map: "serviceTitan_unique")
}

model session {
  id       String  @id @db.VarChar(32)
  data     String? @db.MediumText
  lifetime Int     @db.SmallInt
  modified Int
}

model significantDateTypes {
  id         Int       @id @default(autoincrement()) @db.UnsignedInt
  accountId  Int?      @db.UnsignedInt
  type       String    @db.VarChar(255)
  createdBy  Int?      @db.UnsignedInt
  created    DateTime? @default(now()) @db.Timestamp(0)
  modifiedBy Int?      @db.UnsignedInt
  modified   DateTime? @default(dbgenerated("'0000-00-00 00:00:00'")) @db.DateTime(0)

  @@index([accountId], map: "accountId")
}

model state {
  id           Int     @id @default(autoincrement())
  name         String? @db.VarChar(255)
  abbreviation String  @db.Char(2)
  country      String? @db.VarChar(255)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model stateList {
  stateCode String @db.VarChar(200)
  stateName String @db.VarChar(200)

  @@ignore
}

model states {
  id           Int     @id @default(autoincrement())
  name         String  @db.VarChar(30)
  countryId    Int
  abbreviation String? @db.VarChar(30)

  @@index([abbreviation, name], map: "abbreviation")
  @@index([abbreviation, name], map: "abbreviation_2")
  @@index([countryId], map: "countryId")
}

model stats {
  date_month      DateTime @id @db.Date
  total_revenues  Float?   @db.Float
  new_subscribers Int?
  num_cancelled   Int?
  todays_revenues Float?   @db.Float
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model streakType {
  id         Int       @id @default(autoincrement())
  name       String?   @db.VarChar(255)
  status     Boolean?  @default(true)
  created    DateTime  @default(now()) @db.Timestamp(0)
  createdBy  Int?
  modified   DateTime? @default(now()) @db.Timestamp(0)
  modifiedBy Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model support {
  id              Int     @id @default(autoincrement())
  userId          Int
  accountId       Int
  firstName       String? @db.VarChar(100)
  lastName        String? @db.VarChar(100)
  email           String  @db.VarChar(100)
  feedbackSubject String  @db.VarChar(100)
  feedbackDetails String? @db.MediumText
  status          Boolean @default(true)
}

model test_users {
  id         Int       @id @default(autoincrement()) @db.UnsignedInt
  created_at DateTime? @db.Timestamp(0)
  updated_at DateTime? @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tieredPricingPlanTiers {
  pkId                  Int     @id @default(autoincrement()) @db.UnsignedInt
  tieredPlanId          Int     @db.UnsignedInt
  tierOrder             Int     @db.UnsignedTinyInt
  upToXUser             Int     @default(0) @db.UnsignedInt
  basePrice             Decimal @default(0.00) @db.Decimal(9, 2)
  costPerAdditionalUser Decimal @default(0.00) @db.Decimal(9, 2)
  userCostStripePlanId  String? @db.VarChar(50)

  @@index([tieredPlanId], map: "tieredPlanId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tieredPricingPlans {
  id              Int                         @id @default(autoincrement()) @db.UnsignedInt
  name            String                      @db.VarChar(200)
  description     String                      @db.VarChar(255)
  interval        tieredPricingPlans_interval @default(month)
  stripeProductId String?                     @db.VarChar(100)
  created         DateTime                    @default(now()) @db.Timestamp(0)
}

model timeDimension {
  id          Int      @id
  dbDate      DateTime @unique(map: "tdDbdateIdx") @db.Date
  year        Int
  month       Int
  day         Int
  quarter     Int
  week        Int
  dayName     String   @db.VarChar(9)
  monthName   String   @db.VarChar(9)
  holidayFlag String?  @default("f") @db.Char(1)
  weekendFlag String?  @default("f") @db.Char(1)
  event       String?  @db.VarChar(50)

  @@unique([year, month, day], map: "tdYmdIdx")
}

model twilioNumber {
  id          Int    @id @default(autoincrement())
  countryId   Int
  phoneNumber String @db.VarChar(30)

  @@index([countryId], map: "countryId")
}

model userCappedPercentageInfo {
  id              Int      @id @default(autoincrement())
  userId          Int
  cappedFieldName String   @db.VarChar(50)
  appName         String   @db.VarChar(10)
  functionName    String   @db.VarChar(30)
  created         DateTime @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model userResourcesSentLog {
  pkId             Int      @id @default(autoincrement()) @db.UnsignedInt
  userId           Int      @db.UnsignedInt
  accountId        Int
  type             Boolean  @default(false)
  resourceId       Int      @db.UnsignedInt
  resourceSentDate DateTime @default(now()) @db.DateTime(0)
}

model userResourcesViewedLog {
  pkId               Int      @id @default(autoincrement()) @db.UnsignedInt
  userId             Int      @db.UnsignedInt
  resourceId         Int      @db.UnsignedInt
  resourceViewedDate DateTime @default(now()) @db.DateTime(0)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model userRoles {
  userId                 Int
  roleEntityPermissionId Int
  createdAt              DateTime @db.DateTime(0)
  modifiedAt             DateTime @default(now()) @db.Timestamp(0)

  @@index([roleEntityPermissionId], map: "uRoles_roleEntityPermissionId")
  @@index([userId], map: "uRoles_userId")
  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model userStreakLogs {
  id            Int      @id @default(autoincrement())
  accountId     Int
  userId        Int
  achievementId Int
  count         Int
  streakStatus  Boolean  @default(true)
  created       DateTime @default(now()) @db.Timestamp(0)
  createdBy     Int

  @@index([accountId], map: "accountId")
  @@index([achievementId], map: "achievementId")
  @@index([userId], map: "userId")
}

model userStreakStatus {
  id                 Int      @id @default(autoincrement())
  accountId          Int
  userId             Int
  achievementId      Int
  bestStreakDuration Int
  currStreakDuration Int
  streakDate         DateTime @default(now()) @db.Timestamp(0)
  created            DateTime @default(now()) @db.Timestamp(0)
  createdBy          Int
  modified           DateTime @default(now()) @db.Timestamp(0)
  modifiedBy         Int?

  @@index([accountId], map: "accountId")
  @@index([achievementId], map: "achievementId")
  @@index([streakDate], map: "streakDate")
  @@index([userId], map: "userId")
}

model userWatchedModule {
  id                Int       @unique(map: "id") @default(autoincrement()) @db.UnsignedInt
  accountId         Int?      @db.UnsignedInt
  courseId          Int?      @db.UnsignedInt
  userId            Int?      @db.UnsignedInt
  moduleId          Int?      @db.UnsignedInt
  watchedPercentage Float?    @db.Float
  created           DateTime? @default(now()) @db.Timestamp(0)
  createdBy         Int?      @db.UnsignedInt

  @@index([moduleId], map: "uwm_module_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model users {
  id                  BigInt            @id @default(autoincrement())
  email               String?           @unique(map: "email") @db.VarChar(255)
  new                 Int?              @default(1)
  firstName           String?           @db.VarChar(45)
  lastName            String?           @db.VarChar(45)
  created             DateTime          @default(now()) @db.Timestamp(0)
  createdBy           Int?
  modified            DateTime?         @default(now()) @db.Timestamp(0)
  modifiedBy          Int?
  password            String            @db.Text
  salt                String            @db.Text
  forceChange         Int               @default(1) @db.TinyInt
  accessToken         String?           @db.Text
  roleId              Int               @default(3)
  profileImage        String?           @db.VarChar(45)
  address             String?           @db.VarChar(255)
  suite               String?           @db.VarChar(255)
  city                String?           @db.VarChar(255)
  stateId             Int
  countryId           Int
  zip                 String?           @db.VarChar(20)
  isSuspended         users_isSuspended @default(VALUE_0)
  status              users_status      @default(active)
  associateAccounts   String?           @db.Text
  contactSearchForAcc Int?

  @@index([createdBy], map: "createdBy")
  @@index([modifiedBy], map: "modifiedBy")
  @@index([stateId], map: "stateId")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model visitorShortUrlStats {
  id                Int      @id @default(autoincrement())
  shortUrlId        String?  @db.VarChar(200)
  visitorOs         String?  @db.VarChar(200)
  visitorDeviceType String?  @db.VarChar(200)
  visitorIpAddr     String?  @db.VarChar(200)
  longUrl           String?  @db.MediumText
  shortUrl          String   @db.VarChar(200)
  clickedTime       DateTime @default(now()) @db.Timestamp(0)

  @@index([shortUrl(length: 191)], map: "shortUrl")
}

model webinarTempRecord {
  id              Int      @id @default(autoincrement())
  resources_id    Int?
  contact_id      Int?
  webinarDateTime String?  @db.VarChar(100)
  postWebinarTime String?  @db.VarChar(100)
  preMailSent     Int      @default(0)
  postMailSent    Int      @default(0)
  userMailSent    Int      @default(0)
  status          Int      @default(1)
  accountId       Int      @default(0)
  senderId        Int      @default(0)
  eventType       Int      @default(0)
  createdTime     DateTime @default(now()) @db.Timestamp(0)
  isZoomWebinar   Boolean  @default(false)
  joinedZoom      Boolean  @default(false)
}

enum accountBillingTransactions_type {
  payment_success  @map("payment-success")
  payment_failure  @map("payment-failure")
  payment_refunded @map("payment-refunded")
}

enum accountCampaignContact_channel {
  web
  mobile
}

enum configs_type {
  int
  tinyint
  boolean
  varchar
  enum
  date
  datetime
}

enum accountUserLogs_userRoleId {
  VALUE_1 @map("1")
  VALUE_2 @map("2")
  VALUE_3 @map("3")
  VALUE_4 @map("4")
  VALUE_5 @map("5")
  VALUE_6 @map("6")
}

enum privilege_assert {
  Yes
  No
}

enum prospectRedemptionCode_isUsed {
  VALUE_0 @map("0")
  VALUE_1 @map("1")
}

enum tieredPricingPlans_interval {
  day
  week
  month
  year
}

enum accountBillingPlans_payingUser {
  owner
  user
  groupLeader
}

enum accountBroadcastTempMail_recipientType {
  VALUE_1 @map("1")
  VALUE_2 @map("2")
  VALUE_3 @map("3")
}

enum accountCampaign_showInApp {
  VALUE_0 @map("0")
  VALUE_1 @map("1")
}

enum accountGameLevelAssets_assetType {
  VALUE_1 @map("1")
  VALUE_2 @map("2")
  VALUE_3 @map("3")
  VALUE_4 @map("4")
}

enum contactsBizCardActivityLog_status {
  sent
  clicked_link @map("clicked link")
  opened
}

enum accountBillingProducts_intervalUnit {
  month
  day
}

enum contactsBizCardActivityLog_sentFrom {
  mobile
  web
}

enum eventFollowUpDetails_timeUnit {
  Minutes
  Hours
  Days
}

enum accountCampaignContactEmail_sendStatus {
  VALUE_0 @map("0")
  VALUE_1 @map("1")
  VALUE_2 @map("2")
}

enum accountContactMessageLog_status {
  sent
}

enum accountCourses_status {
  VALUE_0 @map("0")
  VALUE_1 @map("1")
}

enum accountUserIncentive_emailStatus {
  VALUE_0 @map("0")
  VALUE_1 @map("1")
}

enum contactsEventActivityLog_status {
  sent
  clicked_link @map("clicked link")
  opened
  accepted
  declined
}

enum accountCampaign_status {
  VALUE_0 @map("0")
  VALUE_1 @map("1")
}

enum accountContactMessageLog_sentFrom {
  mobile
  web
}

enum accountSystemEmailUserEmail_sendStatus {
  VALUE_0 @map("0")
  VALUE_1 @map("1")
}

enum contactsEventActivityLog_sentFrom {
  mobile
  web
}

enum contactsResourceActivityLog_status {
  sent
  clicked_link   @map("clicked link")
  opened
  call_requested @map("call requested")
  text_requested @map("text requested")
}

enum accountBroadcastEmails_recipientType {
  VALUE_1 @map("1")
  VALUE_2 @map("2")
  VALUE_3 @map("3")
}

enum accountCampaignEmail_status {
  published
  draft
  pending_review @map("pending review")
}

enum accountPreference_enableRepId2 {
  VALUE_0 @map("0")
  VALUE_1 @map("1")
}

enum accountSystemEmails_status {
  VALUE_0 @map("0")
  VALUE_1 @map("1")
}

enum contactsResourceActivityLog_sentFrom {
  mobile
  web
}

enum accountCampaignEmail_isDeleted {
  VALUE_0 @map("0")
  VALUE_1 @map("1")
}

enum accountCollateral_isDeleted {
  VALUE_0 @map("0")
  VALUE_1 @map("1")
}

enum accountMailLog_status {
  VALUE_1 @map("1")
  VALUE_2 @map("2")
  VALUE_3 @map("3")
  VALUE_4 @map("4")
  VALUE_5 @map("5")
  VALUE_6 @map("6")
}

enum accountSystemEmails_isOrFromDefault {
  VALUE_0 @map("0")
  VALUE_1 @map("1")
}

enum accountBillingCustomers_payingUserType {
  user
  owner
  group_leader @map("group-leader")
}

enum accountLevel_status {
  VALUE_0 @map("0")
  VALUE_1 @map("1")
}

enum calendars_show_popdown {
  YES
  NO
}

enum accountResources_showInApp {
  VALUE_0 @map("0")
  VALUE_1 @map("1")
}

enum calendars_display_ads {
  YES
  NO
}

enum courseModules_status {
  VALUE_0 @map("0")
  VALUE_1 @map("1")
}

enum accountResources_status {
  VALUE_0 @map("0")
  VALUE_1 @map("1")
}

enum account_ignoreAccountPayment {
  VALUE_1 @map("1")
  VALUE_0 @map("0")
}

enum accountBillingSubscriptions_intervalUnit {
  month
  day
}

enum accountResources_isPublished {
  VALUE_0 @map("0")
  VALUE_1 @map("1")
}

enum account_trial {
  VALUE_0 @map("0")
  VALUE_1 @map("1")
}

enum users_isSuspended {
  VALUE_0 @map("0")
  VALUE_1 @map("1")
}

enum accountIncentiveProgram_repeatType {
  Weekly
  Monthly
  Annually
}

enum users_status {
  active
  inactive
}

enum account_testAccount {
  VALUE_0 @map("0")
  VALUE_1 @map("1")
}

enum accountContact_new_channel {
  Mobile
  Web_Application @map("Web Application")
  Landing_Page    @map("Landing Page")
  Posting_Code    @map("Posting Code")
}

enum account_trialUserEnabled {
  VALUE_1 @map("1")
  VALUE_0 @map("0")
}

enum accountContact_channel {
  Mobile
  Mobile_Import   @map("Mobile Import")
  Web_Application @map("Web Application")
  Web_Form        @map("Web Form")
  Posting_Code    @map("Posting Code")
  API
  CSV_Import      @map("CSV Import")
}

enum accountUser_status {
  VALUE_0 @map("0")
  VALUE_1 @map("1")
  VALUE_3 @map("3")
}

enum entries1_rsvp {
  YES
  NO
}

enum entries_bkp_rsvp {
  YES
  NO
}

enum accountUser_linkTrackingNotify {
  VALUE_0 @map("0")
  VALUE_1 @map("1")
  VALUE_2 @map("2")
  VALUE_3 @map("3")
}

enum eventsNew_notifyOption {
  VALUE_1 @map("1")
  VALUE_2 @map("2")
  VALUE_3 @map("3")
  VALUE_4 @map("4")
}

enum events_notifyOption {
  VALUE_1 @map("1")
  VALUE_2 @map("2")
  VALUE_3 @map("3")
  VALUE_4 @map("4")
}

enum accountUser_ignoreUserPayment {
  VALUE_1 @map("1")
  VALUE_0 @map("0")
}

enum accountUser_rewardNotifications {
  VALUE_0 @map("0")
  VALUE_1 @map("1")
  VALUE_2 @map("2")
  VALUE_3 @map("3")
}

enum accountUser_emailsFromAdministrators {
  VALUE_0 @map("0")
  VALUE_1 @map("1")
  VALUE_2 @map("2")
  VALUE_3 @map("3")
}

enum accountUser_eventNotifications {
  VALUE_0 @map("0")
  VALUE_1 @map("1")
}

enum accountUser_subscriptionStatus {
  VALUE_1 @map("1")
  VALUE_2 @map("2")
  VALUE_3 @map("3")
}

enum accountUser_userAccessLevel {
  VALUE_0 @map("0")
  VALUE_1 @map("1")
  VALUE_2 @map("2")
  VALUE_3 @map("3")
}
